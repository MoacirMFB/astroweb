---
import Layout from '../../layouts/Layout.astro';

type LinkItem = {
	label: string;
	href: string;
};

type ProjectVideo = {
	url: string;
	start?: number;
};

type ProjectImage = {
	src: string;
	alt: string;
};

type ProjectCard = {
	year: string;
	title: string;
	summary: string;
	highlights?: string[];
	note?: string;
	links?: LinkItem[];
	images?: ProjectImage[];
	video?: ProjectVideo;
	videoInCarousel?: boolean;
};

type ProjectCarouselItem =
	| { type: 'image'; image: ProjectImage }
	| { type: 'video'; video: ProjectVideo };

type AwardItem = {
	title: string;
	href?: string;
};

type LeadershipItem = {
	title: string;
	background: string[];
	links?: LinkItem[];
};

type EducationItem = {
	institution: string;
	programs: string[];
	href?: string;
	logoSrc: string;
	logoAlt: string;
};

type PublicationItem = {
	citation: string;
	venue: string;
	doi?: string;
	url?: string;
	urlLabel?: string;
	links?: LinkItem[];
};

type MediaKind = 'podcast' | 'video' | 'news' | 'official' | 'outreach' | 'profile';

type MediaLinkItem = {
	title: string;
	description: string;
	href: string;
	source: string;
	kind: MediaKind;
	previewImage?: string;
};

type MediaCategory = {
	title: string;
	iconKind: MediaKind;
	links: MediaLinkItem[];
};

type SkillArea = {
	key: string;
	label: string;
	shortLabel: string;
	description: string;
	color: string;
	skills: string[];
};

const navLinks: LinkItem[] = [
	{ label: 'Sobre mí', href: '#about' },
	{ label: 'Proyectos', href: '#portfolio' },
	{ label: 'Premios', href: '#awards' },
	{ label: 'Habilidades', href: '#skills' },
	{ label: 'Liderazgo', href: '#leadership' },
	{ label: 'Educación', href: '#education' },
	{ label: 'Publicaciones', href: '#publications' },
	{ label: 'Medios', href: '#media-interviews' },
	{ label: 'Contacto', href: '#contact' },
];

const heroParagraphs = [
	'Soy Moacir Fonseca Becker, apasionado por el espacio desde que tengo memoria 😃. Mi deseo es el de promover el progreso de la humanidad a través de la innovación y la educación que el desarrollo espacial pueden impulsar.',

	'Tengo una maestría en Aeronáutica y Astronáutica de Purdue University y actualmente curso mi segundo año de doctorado en el mismo campo y universidad. Poseo también una formación dual en ingeniería mecánica y eléctrica,  y he impulsado proyectos espaciales en Centroamérica.', 	'Mi misión es generar un impacto positivo en nuestro mundo contribuyendo al avance de la exploración espacial.',
	'También estoy comprometido con inspirar a la próxima generación. Ofrezco charlas gratuitas sobre ingeniería aeroespacial y mi trayectoria profesional para estudiantes de 15 a 18 años de secundaria. Si te interesa, contáctame.',
];

const getYouTubeId = (url: string): string | null => {
	try {
		const parsed = new URL(url);
		if (parsed.hostname.includes('youtu.be')) {
			return parsed.pathname.replace('/', '') || null;
		}
		if (parsed.hostname.includes('youtube.com')) {
			const video = parsed.searchParams.get('v');
			if (video) {
				return video;
			}
			const parts = parsed.pathname.split('/').filter(Boolean);
			if (parts[0] === 'embed' && parts[1]) {
				return parts[1];
			}
		}
	} catch {
		return null;
	}
	return null;
};

const getGoogleDriveFileId = (url: string): string | null => {
	try {
		const parsed = new URL(url);
		if (!parsed.hostname.includes('drive.google.com')) {
			return null;
		}

		const pathParts = parsed.pathname.split('/').filter(Boolean);
		if (pathParts[0] === 'file' && pathParts[1] === 'd' && pathParts[2]) {
			return pathParts[2];
		}

		const id = parsed.searchParams.get('id');
		return id || null;
	} catch {
		return null;
	}
};

const getEmbedUrl = (video: ProjectVideo): string => {
	const id = getYouTubeId(video.url);
	if (id) {
		const params = new URLSearchParams();
		params.set('rel', '0');
		params.set('modestbranding', '1');
		if (video.start) {
			params.set('start', String(video.start));
		}
		return `https://www.youtube-nocookie.com/embed/${id}?${params.toString()}`;
	}

	const driveId = getGoogleDriveFileId(video.url);
	if (driveId) {
		return `https://drive.google.com/file/d/${driveId}/preview`;
	}

	return video.url;
};

const getProjectCarouselItems = (project: ProjectCard): ProjectCarouselItem[] => {
	const imageItems = (project.images || []).map((image) => ({ type: 'image', image } as const));
	if (project.video && project.videoInCarousel) {
		return [{ type: 'video', video: project.video }, ...imageItems];
	}
	return imageItems;
};

const getYouTubeThumbnailFromUrl = (url: string): string | null => {
	const id = getYouTubeId(url);
	return id ? `https://i.ytimg.com/vi/${id}/hqdefault.jpg` : null;
};

const RECENT_PROJECT_START_YEAR = 2023;

const getProjectStartYear = (year: string): number | null => {
	const match = year.match(/\d{4}/);
	if (!match) {
		return null;
	}
	const parsed = Number(match[0]);
	return Number.isFinite(parsed) ? parsed : null;
};

const getProjectEra = (year: string): 'recent' | 'legacy' => {
	const startYear = getProjectStartYear(year);
	return startYear !== null && startYear >= RECENT_PROJECT_START_YEAR ? 'recent' : 'legacy';
};

const mediaKindLabels: Record<MediaKind, string> = {
	podcast: 'Podcast',
	video: 'Video',
	news: 'Noticias',
	official: 'Oficial',
	outreach: 'Divulgación',
	profile: 'Perfil',
};

const publicationNameSplitRegex =
	/(Becker,\s*M\.F\.|M\.\s+Fonseca\s+Becker|Moacir\s+Fonseca(?:[-\s])?Becker)/gi;
const publicationNameMatchRegex =
	/^(Becker,\s*M\.F\.|M\.\s+Fonseca\s+Becker|Moacir\s+Fonseca(?:[-\s])?Becker)$/i;

const highlightPublicationName = (text: string) =>
	text.split(publicationNameSplitRegex).filter(Boolean).map((segment) => ({
		text: segment,
		isName: publicationNameMatchRegex.test(segment),
	}));

const splitPublicationCitation = (citation: string) => {
	const match = citation.match(/^(.*?\b\d{4}\.\s*)(.+)$/);
	if (!match) {
		return { authors: citation, title: '' };
	}

	return {
		authors: match[1],
		title: match[2],
	};
};

const projects: ProjectCard[] = [
		{
			year: '2025',
			title: 'Gateway State Estimation via Moon-Horizon Optical Navigation',
		summary:
			'Estimacion del estado de Gateway en NRHO usando solo imagenes del horizonte lunar con dinamica CR3BP, reduciendo la dependencia del seguimiento desde tierra.',
		highlights: [
			'Desarrollo de una tuberia de imagenes sinteticas con deteccion de borde lunar y ajuste conico para mediciones de navegacion.',
			'Implementacion de OPNAV de Christian-Robinson con filtrado EKF/UKF.',
			'Logro de precision de posicion a nivel de kilometros y velocidad a nivel de m/s en regimenes cislunares de multiples cuerpos.',
			],
			note: 'Completado como parte de AAE590 PET (2025) en Purdue University.',
				images: [
					{ src: '/projects/AAE590PET1.png', alt: 'AAE590 PET project figure 1' },
					{ src: '/projects/AAE590PET2.png', alt: 'AAE590 PET project figure 2' },
					{ src: '/projects/AAE590PET3.png', alt: 'AAE590 PET project figure 3' },
					{ src: '/projects/AAE590PET4.png', alt: 'AAE590 PET project figure 4' },
					{ src: '/projects/AAE590PET5.png', alt: 'AAE590 PET project figure 5' },
				],
				video: { url: 'https://drive.google.com/file/d/1UX4HTbBpxDPtS9wr6rqP6wO9B7Qv7zbv/view?usp=sharing' },
				videoInCarousel: true,
			},
		{
			year: '2024',
			title: 'Distributed Consensus and Control for Multi-Satellite Debris Rendezvous',
		summary:
			'Diseno de un marco distribuido de estimacion y control para rendezvous cooperativo con desechos grandes bajo dinamica perturbada por J2.',
		highlights: [
			'Desarrollo de EKF distribuido con consenso de Metropolis para un estudio de caso Envisat con 10 naves espaciales.',
			'Convergencia en toda la red en 11 iteraciones con errores medios de 0.33 m en posicion y 0.006 m/s en velocidad.',
			'Implementacion de guiado PD que permitio rendezvous a nivel de metros en 600 s con ΔV inferior a 50 m/s por agente.',
			],
			note: 'Completado como parte de AAE590 MAAC (2024) en Purdue University.',
			images: [
				{ src: '/projects/AAE590MAAC1.png', alt: 'AAE590 MAAC project figure 1' },
				{ src: '/projects/AAE590MAAC2.png', alt: 'AAE590 MAAC project figure 2' },
				{ src: '/projects/AAE590MAAC3.png', alt: 'AAE590 MAAC project figure 3' },
				{ src: '/projects/AAE590MAAC4.png', alt: 'AAE590 MAAC project figure 4' },
				{ src: '/projects/AAE590MAAC5.png', alt: 'AAE590 MAAC project figure 5' },
				{ src: '/projects/AAE590MAAC6.png', alt: 'AAE590 MAAC project figure 6' },
				{ src: '/projects/AAE590MAAC7.png', alt: 'AAE590 MAAC project figure 7' },
			],
		},
		{
			year: '2024',
			title: 'Model Predictive Control for Direct Moon Landing from LPO',
		summary:
			'Desarrollo de un marco MPC convexo para transferencias directas desde orbitas de puntos de libracion hacia sitios de alunizaje lunar objetivos.',
		highlights: [
			'Enfoque en escenarios de alunizaje de precision cerca del polo sur lunar.',
			'Implementacion de restricciones condicionales de colision dentro de un modelo de dinamica CR3BP linealizado.',
			'Demostracion de seguimiento de trayectoria y adaptacion a perturbaciones mediante ajuste de penalizaciones de estado y control.',
			],
			note: 'Completado como parte de AAE590 Advanced Control Applications (2024) en Purdue University.',
			images: [
				{ src: '/projects/AAE590ACA1.png', alt: 'AAE590 ACA project figure 1' },
				{ src: '/projects/AAE590ACA2.png', alt: 'AAE590 ACA project figure 2' },
				{ src: '/projects/AAE590ACA3.png', alt: 'AAE590 ACA project figure 3' },
				{ src: '/projects/AAE590ACA4.jpg', alt: 'AAE590 ACA project figure 4' },
			],
		},
	{
		year: '2023',
		title: 'Orbital pursuer evasion game with incomplete information - class project',
		summary:
			'Este proyecto aborda un juego orbital de persecucion-evasion, donde una nave debe capturar un objetivo en movimiento en orbita con informacion incompleta.',
		highlights: [
			'Uso de estimacion con Filtro de Kalman Unscented para inferir el estado y la estrategia del evasor.',
			'Comparacion entre enfoques de control con informacion completa, juego de suma cero y control informado por UKF.',
			'Modelado de la dinamica con ecuaciones de Euler-Hill para escenarios realistas tipo desechos.',
		],
		note: 'Completado como parte de Optimal Control and Estimation (AAE5980) en Purdue University.',
		video: { url: 'https://youtu.be/qDNzvLLw9oo' },
	},
	{
		year: '2023',
		title: 'Using Cycle-Consistent Adversarial Networks translation to color surface images of Mars',
		summary:
			'Proyecto de aprendizaje automatico enfocado en traducir imagenes de la superficie de Marte a representaciones en color mediante metodos CycleGAN.',
		note: 'Completado como parte de Machine Learning (STAT598 / ECE595) en Purdue University.',
		links: [{ label: 'GitHub Repository', href: 'https://github.com/MoacirMFB/Mars_CycleGAN' }],
		images: [
			{ src: '/projects/A1-1536x887.png', alt: 'CycleGAN Mars translation sample A1' },
			{ src: '/projects/A2-1536x862.png', alt: 'CycleGAN Mars translation sample A2' },
			{ src: '/projects/A3-1536x909.png', alt: 'CycleGAN Mars translation sample A3' },
			{ src: '/projects/B1-1536x856.png', alt: 'CycleGAN Mars translation sample B1' },
			{ src: '/projects/B2-1536x836.png', alt: 'CycleGAN Mars translation sample B2' },
			{ src: '/projects/B3-1536x848.png', alt: 'CycleGAN Mars translation sample B3' },
			{ src: '/projects/C2-1536x761.png', alt: 'CycleGAN Mars translation sample C2' },
			{ src: '/projects/C3-1536x855.png', alt: 'CycleGAN Mars translation sample C3' },
		],
	},
	{
		year: '2023',
		title: 'Distributed machine learning across AI companies - class project',
		summary:
			'Este proyecto estudio aprendizaje automatico distribuido en un contexto de sistemas de sistemas, modelado como empresas de IA colaborando con distintos roles de datos y red.',
		highlights: [
			'Analisis del efecto de la topologia y la diversidad de datos sobre la calidad del modelo.',
			'Implementacion de un proceso de tres fases: entrenamiento local, comparticion de parametros y agregacion.',
			'Observacion de relaciones entre la centralidad de nodos y la variacion en la precision del modelo.',
		],
		note: 'Completado como parte de Systems of Systems (AAE56000) en Purdue University.',
		images: [
			{
				src: '/projects/560_Project_2-1-1024x630.png',
				alt: 'Distributed machine learning architecture diagram',
			},
			{
				src: '/projects/560_Project_1-1-1021x1024.png',
				alt: 'Distributed machine learning results figure',
			},
		],
	},
	{
		year: '2019-22',
		title: 'Morazan Project - the 1st Central American satellite built through regional collaboration',
		summary:
			'Liderazgo en planificacion, requisitos y coordinacion tecnica para un esfuerzo satelital regional en segmentos espacial y terrestre.',
		highlights: [
			'Coordinacion de actores internos y externos, incluyendo JAXA y UNOOSA.',
			'Definicion de alrededor de 200 requisitos de mision, sistema y subsistemas.',
			'Implementacion de flujos seguros de documentacion en la nube y mentoria a equipos de subsistemas.',
		],
		links: [
			{ label: 'Project site', href: 'http://www.proyectomorazan.space/' },
			{ label: 'UNOOSA', href: 'https://www.unoosa.org/' },
			{ label: 'JAXA', href: 'https://global.jaxa.jp/' },
		],
		video: { url: 'https://youtu.be/dFWcI73o9kQ' },
	},
	{
		year: '2018-19',
		title: 'Reusable Space Station mission concept for active debris removal in low Earth orbit',
		summary:
			'Desarrollado durante el ISU Space Studies Program (patrocinado por ESA), este concepto de mision abordo la remocion activa de desechos con principios de ecodiseno e ingenieria concurrente.',
		highlights: [
			'Desarrollo de un diseno Fase-0 para un concepto de manipulador robotico.',
			'Contribucion de representaciones CAD para la arquitectura de mision a nivel de estacion.',
			'Liderazgo en el desarrollo de un caso de negocio viable para remocion de desechos en orbita heliosincrona.',
		],
		links: [
			{
				label: 'Project Library Page',
				href: 'https://isulibrary.isunet.edu/index.php?lvl=notice_display&id=10462',
			},
		],
		video: { url: 'https://youtu.be/fLtRxK0FIfA' },
	},
	{
		year: '2016-17',
		title: 'Effect of non-inertial forces over two robotic arms under reduced (artificial) gravity',
		summary:
			'Tras ganar el 3er ciclo UNOOSA DropTES, este proyecto ejecuto experimentos de robotica bajo microgravedad y condiciones de gravedad inducida por rotacion en ZARM.',
		highlights: [
			'Liderazgo de un equipo multidisciplinario de cinco personas bajo estrictas restricciones tecnicas y de cronograma.',
			'Diseno y ejecucion de un experimento de brazo robotico en el Center of Applied Space Technology and Microgravity.',
			'Presentacion del analisis dinamico resultante en el International Astronautical Congress en Adelaide.',
		],
		links: [
			{
				label: 'Project Page (UNOOSA)',
				href: 'https://www.unoosa.org/oosa/en/ourwork/psa/hsti/capacity-building/droptes-third-cycle.html',
			},
		],
		video: { url: 'https://www.youtube.com/watch?v=8AM3ntJYVpo' },
	},
	{
		year: '2016-17',
		title: 'MultiGAS - autonomous remote volcanic gas monitoring station',
		summary:
			'Diseno, construccion, validacion y despliegue de una estacion autonoma de monitoreo de gases para entornos volcanicos exigentes.',
		highlights: [
			'El sistema midio CO2, H2S, O2, H2, vapor de agua, temperatura y presion.',
			'Combinacion de energia solar y eolica para operacion autonoma de varios anos.',
			'Habilitacion de telemetria a distancias superiores a 40 km.',
		],
		links: [{ label: 'Gaslab YouTube Channel', href: 'https://www.youtube.com/user/GasLabUCR' }],
		video: { url: 'https://youtu.be/BTOVRnA9cMQ' },
	},
	{
		year: '2015-16',
		title: 'MINIGAS LITE - airborne volcanic activity monitoring system',
		summary:
			'Plataforma aerotransportada de monitoreo de gases probada en Kilauea con integracion de telemetria RF.',
		highlights: [
			'Diseno de integracion PCB de sensores CO2, SO2 y H2S sobre STM32-F4.',
			'Construccion de una cadena de telemetria de radiosonda usando Arduino y hardware iMet-1 RSB.',
			'Validacion del flujo de despliegue en globo estratosferico para medicion volcanica.',
		],
		links: [{ label: 'Gaslab YouTube Channel', href: 'https://www.youtube.com/user/GasLabUCR' }],
		video: { url: 'https://www.youtube.com/watch?v=-EJzZnKS7g4' },
	},
	{
		year: '2016',
		title: 'Hexapod Robot Based on Arthropod Locomotion for Difficult Exploration Areas on Mars',
		summary:
			'Proyecto de la primera mision analogica MDRS integrada completamente por Latinoamerica, enfocado en conceptos robustos de locomocion de rover.',
		highlights: [
			'Diseno, integracion y prueba de la estructura mecanica del hexapodo.',
			'Uso de metodos iterativos de optimizacion de ingenieria y manufactura aditiva.',
		],
		links: [
			{
				label: 'Mission Report',
				href: 'http://peru.marssociety.org/wp-content/uploads/2018/02/Team-Peru-and-Team-Latam-Application-Guide-2018.pdf',
			},
		],
		images: [
			{ src: '/projects/Hexapod2-1024x741.png', alt: 'Hexapod robot prototype image 1' },
			{ src: '/projects/Hexapod3-1024x777.jpg', alt: 'Hexapod robot prototype image 2' },
			{
				src: '/projects/Pic-2016-Hexapod-Robot-Based-on-Arthropod-Locomotion-for-Difficult-Exploration-Areas-on-Mars-1.png',
				alt: 'Hexapod robot project field image',
			},
		],
	},
	{
		year: '2015',
		title: 'In-flight control unit for a mass spectrometer and subsystems',
		summary:
			'Diseno y programacion de una unidad de control en vuelo basada en ATmega para instrumentacion de investigacion volcanica.',
		highlights: [
			'Habilitacion de operacion manual y automatica de subsistemas de espectrometro de masas.',
			'Integracion de sensado de temperatura, humedad, presion y flujo de aire.',
			'Gestion del control de bomba turbomolecular y de la distribucion de energia de la mision.',
		],
		links: [{ label: 'Gaslab YouTube Channel', href: 'https://www.youtube.com/user/GasLabUCR' }],
		images: [
			{ src: '/projects/minigas4-1024x818.jpg', alt: 'Mass spectrometer control project image 1' },
			{ src: '/projects/MassSpec1-1024x562.jpg', alt: 'Mass spectrometer control project image 2' },
			{ src: '/projects/minigas1-1024x523.jpg', alt: 'Mass spectrometer control project image 3' },
			{ src: '/projects/MassSpec2-1024x560.jpg', alt: 'Mass spectrometer control project image 4' },
			{ src: '/projects/minigas3-1024x769.jpg', alt: 'Mass spectrometer control project image 5' },
			{ src: '/projects/minigas6-1024x759.jpg', alt: 'Mass spectrometer control project image 6' },
			{ src: '/projects/minigas5-1024x883.jpg', alt: 'Mass spectrometer control project image 7' },
		],
	},
	{
		year: '2015-16',
		title: 'Controller box for auto/manual control of hydraulic and pneumatic circuits',
		summary:
			'Desarrollo de un modulo multiproposito de salidas por relevadores para sistemas hidraulicos y neumaticos de laboratorio con automatizacion programable.',
		highlights: [
			'Integracion de una interfaz de usuario en LabVIEW para configuracion y operacion.',
			'Soporte de operacion autonoma sin supervision durante varios dias.',
		],
		links: [{ label: 'NTCR Consulting', href: 'http://www.ntcrconsulting.com/' }],
		images: [{ src: '/projects/GasLab-p2-1-1024x768.jpg', alt: 'Controller box system hardware image' }],
	},
	{
		year: '2014',
		title: 'Blink-controlled small prototype of a remote-controlled car',
		summary:
			'Diseno de un prototipo de carro a control remoto accionado por actividad EEG y comandos por parpadeo.',
		highlights: [
			'Procesamiento de EEG en tiempo real con transformadas de Fourier para deteccion de estados mentales.',
			'Implementacion de logica de control para operar el vehiculo mediante parpadeos.',
			'Inspiracion para desarrollos de tecnologia asistiva dirigidos a ninos con discapacidad fisica.',
		],
		links: [{ label: 'NTCR Consulting', href: 'http://www.ntcrconsulting.com/' }],
		video: { url: 'https://youtu.be/eaAdc4N_em0' },
	},
	{
		year: '2015',
		title: 'Launch of the first solid-state rocket with a scientific payload in Costa Rica',
		summary:
			'Participacion en el lanzamiento de una mision con carga util cientifica para medir variables atmosfericas en Costa Rica.',
		links: [
			{
				label: 'University article',
				href: 'https://www.ucr.ac.cr/noticias/2015/07/31/futuros-cientificos-costarricenses-ensamblan-y-encienden-sus-primeros-cohetes.html',
			},
		],
		video: { url: 'https://www.youtube.com/watch?v=z9DcP4HorTg' },
	},
];

const awards: AwardItem[] = [
	{ title: '2024 - Premio Somos Purdue' },	
	{
		title:
			'2023 - Miembro seleccionado para la primera tripulación del Student Analog Astronaut Training Program (Purdue SEARCH)',
		href: 'https://www.linkedin.com/posts/moacirfb_satp-search-mars-activity-7088241964965326848-au9c?utm_source=share&utm_medium=member_desktop',
	},
	{
		title: '2020 - Ganador de la 5ta ronda de KiboCube (UNOOSA y JAXA)',
		href: 'https://www.unoosa.org/oosa/en/informationfor/media/winner-of-5th-round-of-the-kibocube-programme-to-deploy-a-satellite-from-the-international-space-station-announced-and-opening-of-6th-round.html',
	},
	{ title: '2018 - Ganador del Business Pitch Competition, Departamento de Business and Management (ISU)' },
	{ title: '2018 - Homenaje (graduación en mi honor) - Colegio Colegio Científico de Pérez Zeledón' },
	{ title: '2017 - Finalista, Singularity University Costa Rica Global Impact Challenge' },
	{
		title: '2016 - Ganador de la 3ra campana, UNOOSA DropTES Fellowship Program',
		href: 'https://www.unoosa.org/oosa/en/ourwork/psa/hsti/capacity-building/droptes-third-cycle.html',
	},
	{
		title: '2014 - 1er lugar en RobotiFest, concurso nacional de robotica',
		href: 'https://www.ucr.ac.cr/noticias/2014/08/19/grandes-proyectos-salen-a-la-luz-en-robotifest-ucr.html',
	},
	{ title: '2013 y 2014 - Beca de excelencia academica, Universidad de Costa Rica' },
	{
		title: '2012 - Becario del US Department of State, programa Global UGRAD',
		href: 'https://issuu.com/worldlearning/docs/3._2012_-_2013_yearbook',
	},
	{
		title: '2012 y 2014 - 2do y 3er lugar en la Olimpiada Nacional de Robotica',
		href: 'https://www.micit.go.cr/portaldos/index.php?option=com_content&view=article&id=6228:olimpiada-mundial-de-robotica-abre-las-puertas-a-talento-tico&catid=40&Itemid=1917',
	},
	{
		title: '2009 y 2010 - Mencion honorifica en las Olimpiadas Costarricenses de Fisica XII y XIII',
		href: 'https://www.perezzeledon.net/generalenos-se-preparan-para-olimpiada-iberoamericana-de-fisica/',
	},
	{ title: '2010 - Finalista de la Olimpiada de Matematica' },
	{ title: '2010 - Ganador de la feria cientifica regional y finalista de la feria cientifica nacional' },
];

const leadership: LeadershipItem[] = [
	{
		title: '2020 - Proyecto Morazan, primer satelite hondureno',
		background: [
			'Creacion de planes de trabajo iniciales, modelos de organizacion y cronogramas para casi 40 estudiantes distribuidos en los distintos segmentos del proyecto.',
			'Coordinacion de reuniones quincenales con subequipos para dar seguimiento al progreso, orientar el trabajo y mantener el ritmo de entrega.',
		],
	},
	{
		title: '2019 - Organizador del 1er Central American Space Generation Workshop',
		background: [
			'Gestion de comunicaciones con delegados, moderacion de reuniones, invitacion de conferencistas, coordinacion de patrocinadores y apoyo para lograr una participacion record de aproximadamente 150 delegados.',
		],
	},
	{
		title: '2016 - DropTES 3ra ronda, coordinador del equipo de investigacion',
		background: [
			'Propuesta del experimento de microgravedad ganador de UNOOSA DropTES y coordinacion del alcance del equipo, cronograma y restricciones de alistamiento.',
		],
	},
	{
		title: '2015 - Miembro del staff del 1er campamento costarricense de coheteria',
		background: [
			'Apoyo a la organizacion del evento, guia en construccion y acompanamiento a participantes en actividades de coheteria de modelo.',
		],
	},
	{
		title: '2012 - Grupo de robotica, Universidad de Costa Rica',
		background: [
			'Cofundacion del primer grupo de robotica en la UCR para crear espacio compartido, mentoria e infraestructura para proyectos estudiantiles de robotica.',
		],
	},
];

const education: EducationItem[] = [
	{
		institution: 'Purdue University',
		href: 'https://www.purdue.edu/',
		logoSrc: '/logos/purdue.png',
		logoAlt: 'Purdue University logo',
		programs: [
			'Estudiante de doctorado en Aeronautica y Astronautica (2022 - actualidad)',
			'GPA: 3.88',
		],
	},
	{
		institution: 'International Space University',
		href: 'https://www.isunet.edu/',
		logoSrc: '/logos/isu.png',
		logoAlt: 'International Space University logo',
		programs: ['31st Space Studies Program (2018)', 'Minor en Business and Management', 'GPA: 4.0'],
	},
	{
		institution: 'University of Costa Rica',
		href: 'https://www.ucr.ac.cr/',
		logoSrc: '/logos/ucr.png',
		logoAlt: 'University of Costa Rica logo',
		programs: [
			'Ingenieria mecanica (2011-2016)',
			'Minor en ingenieria electrica',
			'Top 5% de la generacion (2/42)',
		],
	},
	{
		institution: 'University of Minnesota',
		href: 'https://twin-cities.umn.edu/',
		logoSrc: '/logos/minnesota.png',
		logoAlt: 'University of Minnesota logo',
		programs: [
			'Ingenieria mecanica (H2 2012)',
			'Un semestre de intercambio mediante una beca del US Department of State',
		],
	},
];

const skillAreas: SkillArea[] = [
	{
		key: 'software',
		label: 'Software',
		shortLabel: 'Software',
		description: 'Programming and machine-learning implementation for technical problem solving.',
		color: '#21c8ff',
		skills: ['Python', 'Java', 'C', 'Machine Learning', 'MATLAB'],
	},
	{
		key: 'modeling',
		label: 'Modeling and Simulation',
		shortLabel: 'Modeling',
		description: 'System modeling and simulation methods for mission architecture and analysis.',
		color: '#ff9f1c',
		skills: ['AnyLogic', 'GENESYS', 'Simulation', 'Optimal Control', 'Astrodynamics'],
	},
	{
		key: 'mechanical',
		label: 'Mechanical and Aerospace',
		shortLabel: 'Mechanical',
		description: 'Mechanical design and aerospace system engineering from concept through prototyping.',
		color: '#06d6a0',
		skills: [
			'SolidWorks',
			'Inventor',
			'Fusion',
			'AutoCAD',
			'3D Printing',
			'Aerospace Systems',
			'Satellite Design',
		],
	},
	{
		key: 'electrical',
		label: 'Electrical and Embedded',
		shortLabel: 'Electrical',
		description: 'Electronics design, embedded integration, sensing, and field-ready hardware work.',
		color: '#ff4d6d',
		skills: ['KiCAD', 'OrCAD', 'Data Acquisition', 'Embedded Systems', 'Networking', 'Soldering', 'RTOS'],
	},
	{
		key: 'platforms',
		label: 'Platforms and Workflow',
		shortLabel: 'Platforms',
		description: 'Tooling used for deployment, analysis workflows, documentation, and collaboration.',
		color: '#b8f12c',
		skills: ['GNU/Linux', 'LaTeX', 'LabVIEW', 'Spreadsheets', 'WordPress', 'Google Cloud'],
	},
];

const defaultSkillArea = skillAreas[0]!;
const skillAreaPayload = skillAreas.map((area) => ({
	key: area.key,
	label: area.label,
	shortLabel: area.shortLabel,
	description: area.description,
	color: area.color,
	skills: area.skills,
	count: area.skills.length,
}));

const publications: PublicationItem[] = [
	{
		citation:
			'M. Fonseca Becker, K. Hernández Bandrich, N. E. May, S. M. Patwardhan, A. Massini, and A. Capannolo, 2026. Virtual Thrust-Based Control Formulation for Multiple Kinetic Impactor Asteroid Deflection.',
		venue: '30th International Symposium on Space Flight Dynamics (ISSFD), Toulouse, France, 2026.',
		links: [
			{ label: 'Detalles', href: '/publications/virtual-thrust-issfd-2026/' },
			{
				label: 'PDF',
				href: '/publications/virtual-thrust-issfd-2026/virtual-thrust-issfd-2026.pdf',
			},
		],
	},
	{
		citation:
			'A. Massini, N. E. May, M. Fonseca Becker, and A. Capannolo, 2026. Earth-Moon Orbit Design for a Rapid-Response Asteroid Interceptor System.',
		venue: '30th International Symposium on Space Flight Dynamics (ISSFD), Toulouse, France, 2026.',
		links: [
			{ label: 'Detalles', href: '/publications/earth-moon-interceptor-issfd-2026/' },
			{
				label: 'PDF',
				href: '/publications/earth-moon-interceptor-issfd-2026/earth-moon-interceptor-issfd-2026.pdf',
			},
		],
	},
	{
		citation:
			'Schmitt, R.N., Becker, M.F., DeLaurentis, D., Capannolo, A., 2025. An Explainability-Based Framework for Evaluating Space Mission Architectures Using Sandbox Games.',
		venue: 'Aerotecnica Missili & Spazio, pp. 1-26.',
		doi: '10.1007/s42496-025-00293-1',
	},
	{
		citation:
			'Schmitt, R.N., Becker, M.F., DeLaurentis, D., Oguri, K., 2025. Design of Space Systems and Operational Mission Architectures Using Sandbox Games.',
		venue: 'Acta Astronautica.',
		doi: '10.1016/j.actaastro.2025.04.004',
	},
	{
		citation:
			'Becker, M.F., Reynel, G., Francisco, S., Mauricio, A., Eduardo, G., Fernando, Z., Luis, M., Carlos, A., Maria Jose, M., Oliver, S., 2022. Morazan Project Ground and Space Systems: Results of a Successful Preliminary Design for Space and Ground Segments, International Cooperation, Knowledge Transfer and Lessons Learned.',
		venue: 'In: 73rd International Astronautical Congress, Paris.',
		url: 'https://iafastro.directory/iac/paper/id/59527/summary/',
		urlLabel: 'IAC summary',
	},
	{
		citation:
			'Javier, M., Vilma, O., Eduardo, G., Fernando, Z., Becker, M.F., Luis, M., Carlos, A., Maria Jose, M., Oliver, S., 2020. Cabanas: First Honduran Academic Ground Station for small satellite missions.',
		venue: 'In: 71st International Astronautical Congress.',
		url: 'https://iafastro.directory/iac/paper/id/59527/summary/',
		urlLabel: 'IAC summary',
	},
	{
		citation:
			'Javier, M., Becker, M.F., Luis, M., Eduardo, G., Maria Jose, M., Oliver, S., Carlos E., A., Fernando, Z., 2020. Paving the way to the stars: Inclusion and education through space.',
		venue: 'In: International Astronautical Congress Cyberspace Edition. International Astronautical Federation.',
	},
	{
		citation:
			'Becker, M.F., Aureliano, R., Alexander, O., Shirrel, A., Yanina, H., Rudiger, J., Olga, Z., Singh, P., Tiago, S., 2019. Space Station Concept for Active Debris Removal Applying EcoDesign Principles.',
		venue: 'In: 70th International Astronautical Congress.',
	},
	{
		citation:
			'Luis, M., Becker, M.F., Carlos E., A., Maria Jose, M., Fernando, Z., Javier, M., Eduardo, G., Oliver, S., 2019. Morazan MRZ-SAT CubeSat project for integration of the Central American Nations through collaboration in Space.',
		venue: 'In: 70th International Astronautical Congress. International Astronautical Federation.',
	},
	{
		citation:
			'Becker, M.F., Nicole, C., Carlos, M., Renato, R., Ernesto, C., 2017. Analysis of scaled robotic arm manipulators under microgravity conditions.',
		venue: 'In: 68th International Astronautical Congress. Adelaide: International Astronautical Federation.',
	},
];

const externalMediaCategories: MediaCategory[] = [
	{
		title: 'Podcasts y Entrevistas',
		iconKind: 'podcast',
		links: [
			{
				title: 'epiSTEMas Episode E061',
				description:
					'Conversacion en podcast sobre ingenieria aeroespacial y la participacion centroamericana en el espacio.',
				href:
					'https://epistemaspod.podbean.com/e/moacir-fonseca-becker-ingenieria-mecanica-y-centroamerica-en-el-espacio/',
				source: 'EpiSTEMas',
				kind: 'podcast',
				previewImage: 'https://i.ytimg.com/vi/oIhVfwIRsbI/hqdefault.jpg',
			},
			{
				title: 'UNOOSA LEAD Youth Innovation Festival Panel',
				description: 'Segmento de panel con la Oficina de las Naciones Unidas para Asuntos del Espacio Ultraterrestre.',
				href: 'https://www.youtube.com/watch?v=gW7j54dQGX0&t=970s',
				source: 'YouTube / UNOOSA',
				kind: 'video',
			},
			{
				title: 'La Reaccion CR Interview',
				description: 'Entrevista y conversacion en medios en idioma espanol.',
				href: 'https://www.youtube.com/watch?v=UKomvqgaHiU&t=402s',
				source: 'YouTube',
				kind: 'video',
			},
		],
	},
	{
		title: 'Noticias y Articulos Destacados',
		iconKind: 'news',
		links: [
			{
				title: 'How Kerbal Space Program Is Inspiring Real Mission Designs',
				description: 'Articulo destacado que menciona a Moacir F. Becker en contexto de diseno de misiones.',
				href: 'https://www.universetoday.com/articles/how-kerbal-space-program-is-inspiring-real-mission-designs',
				source: 'Universe Today',
				kind: 'news',
			},
			{
				title: 'Robotifest Projects at UCR',
				description: 'Articulo universitario sobre proyectos de robotica y resultados de competencia.',
				href:
					'https://www.ucr.ac.cr/noticias/2014/8/19/grandes-proyectos-salen-a-la-luz-en-robotifest-ucr/imprimir.html?empotrar=true&ruta=https%3A%2F%2Fwww.ucr.ac.cr%2Fnoticias%2F2014%2F8%2F19%2Fgrandes-proyectos-salen-a-la-luz-en-robotifest-ucr.html',
				source: 'Universidad de Costa Rica',
				kind: 'news',
			},
			{
				title: 'DropTES Third Cycle Overview',
				description: 'Resumen oficial del tercer ciclo de DropTES y de los equipos participantes.',
				href:
					'https://www.unoosa.org/oosa/en/ourwork/psa/hsti/capacity-building/droptes-third-cycle.html',
				source: 'UNOOSA',
				kind: 'news',
			},
		],
	},
	{
		title: 'Paginas Oficiales e Institucionales',
		iconKind: 'official',
		links: [
			{
				title: 'UNOOSA Hypergravity and Microgravity Webinar Track',
				description: 'Referencias de ganadores de DropTES con materiales de webinar.',
				href: 'https://www.unoosa.org/oosa/en/ourwork/access2space4all/HMTrack_Webinars.html',
				source: 'UNOOSA',
				kind: 'official',
			},
			{
				title: 'UNOOSA Awardees: Costa Rica TEC and UCR',
				description: 'Listado institucional de galardonados para participantes de Costa Rica.',
				href:
					'https://www.unoosa.org/oosa/en/ourwork/access2space4all/awardees/costa_rica_tec_ucr.html',
				source: 'UNOOSA',
				kind: 'official',
			},
			{
				title: 'UNOOSA Common Webinars',
				description: 'Indice de webinars con referencias a DropTES y medios relacionados.',
				href: 'https://www.unoosa.org/oosa/en/ourwork/access2space4all/Common_Webinars.html',
				source: 'UNOOSA',
				kind: 'official',
			},
			{
				title: 'MRZ-SAT Project Site',
				description: 'Pagina oficial de la mision con informacion del equipo y del proyecto.',
				href: 'https://mrz-sat.unah.edu.hn/english/',
				source: 'Proyecto Morazan / UNAH',
				kind: 'official',
			},
		],
	},
	{
		title: 'Charlas, Divulgacion y Perfiles',
		iconKind: 'outreach',
		links: [
			{
				title: 'Purdue TicoTalk Event',
				description: 'Publicacion de evento en BoilerLink con participacion de Moacir Fonseca Becker.',
				href: 'https://boilerlink.purdue.edu/event/11509502',
				source: 'Purdue BoilerLink',
				kind: 'outreach',
			},
			{
				title: 'Educational Outreach: Colegio Cientifico de San Vito',
				description: 'Publicacion de divulgacion para estudiantes de secundaria sobre trayectorias en aeroespacial.',
				href:
					'https://www.facebook.com/CCCRSV/posts/el-joven-moacir-fonseca-becker-es-estudiante-de-posgrado-de-ingenier%C3%ADa-aeroespac/711624457433119/',
				source: 'Facebook',
				kind: 'outreach',
			},
			{
				title: 'Google Scholar Profile',
				description: 'Perfil academico con citas y trayectoria de investigacion.',
				href: 'https://scholar.google.com/citations?hl=en&user=wc3XeSoAAAAJ',
				source: 'Google Scholar',
				kind: 'profile',
			},
			{
				title: 'Official Personal Site',
				description: 'Sitio personal principal y portafolio.',
				href: 'https://moacir.space/',
				source: 'moacir.space',
				kind: 'profile',
			},
		],
	},
];
---

<Layout
	title="Moacir Fonseca Becker"
	lang="es"
	description="Innovación impulsada por el espacio, proyectos aeroespaciales y oportunidades de colaboración."
>
	<main class="page">
		<div class="aurora" aria-hidden="true">
			<span class="bg-image-layer"></span>
			<span class="orb orb-one"></span>
			<span class="orb orb-two"></span>
			<span class="orb orb-three"></span>
		</div>

		<header class="site-header">
			<a class="brand" href="#about" aria-label="Ir al inicio">
				<span class="brand-name">Moacir Fonseca Becker</span>
			</a>
			<nav aria-label="Navegación principal">
				<ul>
					{
						navLinks.map((item) => (
							<li><a href={item.href}>{item.label}</a></li>
						))
					}
				</ul>
			</nav>
			<div class="header-actions">
				<button
					type="button"
					class="theme-toggle"
					data-theme-toggle
					aria-label="Cambiar a modo claro"
					title="Cambiar a modo claro"
				>
					<span class="theme-icon theme-icon--sun" aria-hidden="true">
						<svg viewBox="0 0 24 24" role="presentation">
							<circle cx="12" cy="12" r="3.5"></circle>
							<path d="M12 1.8v2.8"></path>
							<path d="M12 19.4v2.8"></path>
							<path d="m4.1 4.1 2 2"></path>
							<path d="m17.9 17.9 2 2"></path>
							<path d="M1.8 12h2.8"></path>
							<path d="M19.4 12h2.8"></path>
							<path d="m4.1 19.9 2-2"></path>
							<path d="m17.9 6.1 2-2"></path>
						</svg>
					</span>
					<span class="theme-icon theme-icon--moon" aria-hidden="true">
						<svg viewBox="0 0 24 24" role="presentation">
							<path d="M20.6 14.5a8.8 8.8 0 1 1-11-11 7.2 7.2 0 1 0 11 11Z"></path>
						</svg>
					</span>
				</button>
				<a
					class="language-toggle"
					href="/"
					aria-label="Cambiar a inglés"
					data-language-cue="Cambiar idioma"
				>
					🇺🇸
				</a>
				<a
					class="cv-link"
					href="https://drive.google.com/file/d/1vwPQEI_GvsCVWiBJQ5meD143yRfUaTRw/view?usp=sharing"
					target="_blank"
					rel="noreferrer"
				>
					Currículum
				</a>
			</div>
		</header>

		<section id="about" class="hero">
			<p class="eyebrow">
				<span class="eyebrow-flags" aria-hidden="true">🇺🇸 🇧🇷 🇨🇷</span>
				<span class="eyebrow-greetings">HELLO!  OLÁ!  HOLA!</span>
			</p>
			<h1>Moacir Fonseca Becker</h1>
			<div class="hero-main">
				<div class="hero-copy">
					{heroParagraphs.map((paragraph) => <p>{paragraph}</p>)}
				</div>
				<aside class="hero-photo-wrap">
					<img
						class="hero-photo"
						src="/profile/moacir-photo.jpg"
						alt="Portrait of Moacir Fonseca Becker"
						loading="eager"
						decoding="async"
					/>
				</aside>
			</div>
			<p class="thank-you">¡Gracias por visitar mi página!</p>
		</section>

		<section id="portfolio" class="section-shell">
				<div class="section-header">
					<h2>Portafolio de Proyectos</h2>
				</div>

			<div class="portfolio-toolbar" role="toolbar" aria-label="Filtros de proyectos">
				<button type="button" class="filter-button is-active" data-filter="all">Todos los Proyectos</button>
				<button type="button" class="filter-button" data-filter="recent">{`Recientes (${RECENT_PROJECT_START_YEAR}+)`}</button>
				<button type="button" class="filter-button" data-filter="legacy">Anteriores</button>
				<button type="button" class="filter-button" data-filter="video">Con Video</button>
			</div>
			<p class="portfolio-count" data-project-count>Mostrando {projects.length} proyectos</p>

			<div class="project-grid">
				{
					projects.map((project) => (
						<article
							class="project-card"
							data-era={getProjectEra(project.year)}
							data-has-video={project.video ? 'true' : 'false'}
						>
								<div class="card-header">
									<p class="year">{project.year}</p>
									{project.video && <span class="video-chip">Vista previa en video</span>}
								</div>
								<h3>{project.title}</h3>
								<p>{project.summary}</p>
								{
									project.highlights && (
										<div class="project-contrib">
											<button type="button" class="project-contrib-trigger">Aportes clave</button>
											<div class="project-contrib-popover" role="tooltip">												
												<ul class="project-contrib-list">
												{project.highlights.map((item) => (
													<li>{item}</li>
												))}
												</ul>
											</div>
										</div>
									)
								}
								{
									(() => {
										const carouselItems = getProjectCarouselItems(project);
										if (carouselItems.length === 0) {
											return null;
										}
										return (
											<div class="project-gallery-wrap">
												<div class="project-carousel" data-carousel>
													<button
														type="button"
														class="carousel-control"
														data-carousel-prev
														aria-label={`Elemento anterior de ${project.title}`}
													>
														‹
													</button>
														<div class="project-gallery" role="region" aria-label={`Carrusel multimedia de ${project.title}`}>
															{carouselItems.map((item, index) => (
																<figure
																	class={`gallery-item ${item.type === 'video' ? 'gallery-item--video ' : ''}${index === 0 ? 'is-active' : ''}`}
																	data-carousel-slide
																	data-lightbox-kind={item.type}
																	data-lightbox-src={item.type === 'video' ? getEmbedUrl(item.video) : item.image.src}
																	data-lightbox-alt={item.type === 'video' ? `Vista previa en video de ${project.title}` : item.image.alt}
																	aria-hidden={index === 0 ? 'false' : 'true'}
																>
																	{
																		item.type === 'video' ? (
																			(() => {
																				const posterSrc = getYouTubeThumbnailFromUrl(item.video.url) || project.images?.[0]?.src || '';
																				return (
																					<div
																						class="gallery-video-wrap"
																						data-gallery-video-open
																						role="button"
																						tabindex="0"
																						aria-label={`Haz clic para ampliar el video de ${project.title}`}
																					>
																						{
																							posterSrc ? (
																								<img
																									class="gallery-video-thumb"
																									src={posterSrc}
																									alt={`Miniatura de video de ${project.title}`}
																									loading="eager"
																									decoding="async"
																								/>
																							) : (
																								<div class="gallery-video-fallback">{project.title}</div>
																							)
																						}
																						<span class="gallery-video-play" aria-hidden="true"></span>
																					</div>
																				);
																			})()
																		) : (
																		<img
																			class="gallery-image"
																			src={item.image.src}
																			alt={item.image.alt}
																			loading="eager"
																			decoding="async"
																		/>
																	)
																}
															</figure>
														))}
													</div>
													<button
														type="button"
														class="carousel-control"
														data-carousel-next
														aria-label={`Elemento siguiente de ${project.title}`}
													>
														›
													</button>
													<div class="carousel-dots" role="tablist" aria-label={`${project.title} media selection`}>
														{
															carouselItems.map((_, index) => (
																<button
																	type="button"
																	class={`carousel-dot ${index === 0 ? 'is-active' : ''}`}
																	data-carousel-dot
																	aria-label={`Ir al elemento ${index + 1}`}
																	aria-selected={index === 0 ? 'true' : 'false'}
																></button>
															))
														}
													</div>
												</div>
											</div>
										);
									})()
								}
							{project.note && <p class="note">{project.note}</p>}
							{
								project.links && (
									<ul class="resource-list">
										{project.links.map((link) => (
											<li>
												<a class="pill-link" href={link.href} target="_blank" rel="noreferrer">
													{link.label}
												</a>
											</li>
										))}
									</ul>
								)
							}
							{
								project.video && !project.videoInCarousel && (
									<div class="video-wrap">
										<iframe
											title={`Vista previa en video de ${project.title}`}
											src={getEmbedUrl(project.video)}
											loading="lazy"
											allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
											referrerpolicy="strict-origin-when-cross-origin"
											allowfullscreen
										></iframe>
									</div>
								)
							}
						</article>
					))
				}
			</div>
		</section>

		<section id="awards" class="section-shell">
			<div class="section-header">
				<h2>Logros, Premios y Reconocimientos</h2>
			</div>
			<ul class="award-list">
				{
					awards.map((award) => (
						<li>
							{award.href ? (
								<a href={award.href} target="_blank" rel="noreferrer">{award.title}</a>
							) : (
								<span>{award.title}</span>
							)}
						</li>
					))
				}
			</ul>
		</section>

		<section id="skills" class="section-shell">
			<div class="section-header">
				<h2>Habilidades</h2>
			</div>
			<div class="skills-chart-shell" data-skills-chart>
				<div class="skills-visual">
					<svg
						class="skills-sunburst"
						viewBox="0 0 340 340"
						role="img"
						aria-label="Gráfico interactivo de habilidades"
						data-skills-sunburst
					></svg>
				</div>

				<div class="skills-panel">
					<ul class="skills-legend">
						{
							skillAreas.map((area, index) => (
								<li>
									<button
										type="button"
										class:list={['legend-button', index === 0 && 'is-active']}
										data-skill-key={area.key}
										aria-pressed={index === 0 ? 'true' : 'false'}
									>
										<span class="legend-swatch" style={`--legend-color: ${area.color};`}></span>
										<span class="legend-title">{area.label}</span>
										<span class="legend-meta">{area.skills.length}</span>
									</button>
								</li>
							))
						}
					</ul>

					<div class="skills-active-copy">
						<p class="skills-active-title" data-skill-active-title>{defaultSkillArea.label}</p>
						<p class="skills-active-description" data-skill-active-description>
							{defaultSkillArea.description}
						</p>
						<ul class="skills-tags" data-skill-active-list>
							{defaultSkillArea.skills.map((skill) => <li>{skill}</li>)}
						</ul>
					</div>
				</div>
			</div>
				<script
					type="application/json"
					id="skills-area-data"
					is:inline
					set:html={JSON.stringify(skillAreaPayload)}
				></script>
			</section>

		<section id="leadership" class="section-shell">
			<div class="section-header">
				<h2>Liderazgo</h2>
			</div>
			<div class="leadership-grid">
				{
					leadership.map((item) => (
						<article class="leadership-card">
							<h3>{item.title}</h3>
							<ul class="detail-list">
								{item.background.map((line) => (
									<li>{line}</li>
								))}
							</ul>
							{
								item.links && (
									<ul class="resource-list">
										{item.links.map((link) => (
											<li>
												<a href={link.href} target="_blank" rel="noreferrer">{link.label}</a>
											</li>
										))}
									</ul>
								)
							}
						</article>
					))
				}
			</div>
		</section>

		<section id="education" class="section-shell">
			<div class="section-header">
				<h2>Educación</h2>
			</div>
			<div class="education-grid">
				{
					education.map((item) => (
						<article class="education-card">
								<div class="logo-frame">
									<img src={item.logoSrc} alt={item.logoAlt} loading="eager" />
								</div>
							<h3>{item.institution}</h3>
							<ul class="detail-list">
								{item.programs.map((program) => (
									<li>{program}</li>
								))}
							</ul>
														
						</article>
					))
				}
			</div>
		</section>

			<section id="publications" class="section-shell">
				<div class="section-header">
					<h2>Publicaciones</h2>
				</div>
					<ol class="publication-list">
						{
							publications.map((item, index) => {
								const parsedCitation = splitPublicationCitation(item.citation);
								const links = item.links || [];
								return (
									<li class="publication-item">
										<p class="publication-line">
											<span class="publication-index">[{index + 1}]</span>{' '}
											<span class="publication-citation">
												<span class="publication-authors">
													{highlightPublicationName(parsedCitation.authors).map((segment) =>
														segment.isName ? (
															<strong class="publication-author-highlight">{segment.text}</strong>
														) : (
															<span>{segment.text}</span>
														)
													)}
												</span>{' '}
												{parsedCitation.title && (
													<span class="publication-paper-title">{parsedCitation.title}</span>
												)}
											</span>{' '}
											<span class="publication-venue">{item.venue}</span>
											{(item.doi || item.url || links.length > 0) && (
												<span class="publication-links">
													{' '}
													Enlaces:{' '}
													{item.doi && (
														<>
															DOI:{' '}
															<a href={`https://doi.org/${item.doi}`} target="_blank" rel="noreferrer">
																{item.doi}
															</a>
															{(item.url || links.length > 0) && ' · '}
														</>
													)}
													{item.url && (
														<>
															<a href={item.url} target="_blank" rel="noreferrer">
																{item.urlLabel || 'Enlace'}
															</a>
															{links.length > 0 && ' · '}
														</>
													)}
													{links.map((link, index) => (
														<>
															{index > 0 && ' · '}
															<a href={link.href}>{link.label}</a>
														</>
													))}
												</span>
											)}
										</p>
									</li>
								);
							})
						}
					</ol>
			</section>

			<section id="media-interviews" class="section-shell">
				<div class="section-header">
					<h2>Enlaces Externos en Medios</h2>					
				</div>
				<svg class="media-icon-defs" aria-hidden="true" focusable="false">
					<symbol id="icon-podcast" viewBox="0 0 24 24">
						<rect x="9" y="3" width="6" height="10" rx="3"></rect>
						<path d="M6 10.5a6 6 0 0 0 12 0"></path>
						<line x1="12" y1="17" x2="12" y2="21"></line>
						<line x1="9" y1="21" x2="15" y2="21"></line>
					</symbol>
					<symbol id="icon-video" viewBox="0 0 24 24">
						<rect x="3" y="5" width="18" height="14" rx="2"></rect>
						<polygon points="10,9 16,12 10,15"></polygon>
					</symbol>
					<symbol id="icon-news" viewBox="0 0 24 24">
						<rect x="3" y="4" width="18" height="16" rx="2"></rect>
						<line x1="7" y1="8" x2="17" y2="8"></line>
						<line x1="7" y1="12" x2="17" y2="12"></line>
						<line x1="7" y1="16" x2="13" y2="16"></line>
					</symbol>
					<symbol id="icon-official" viewBox="0 0 24 24">
						<path d="M12 3l7 3v5c0 5-3.2 8.6-7 10-3.8-1.4-7-5-7-10V6l7-3z"></path>
					</symbol>
					<symbol id="icon-outreach" viewBox="0 0 24 24">
						<path d="M3 12l10-5v10L3 12z"></path>
						<rect x="13" y="10" width="3" height="4" rx="1"></rect>
						<path d="M16 10.5c2 0 3.5-1.3 4.5-2.5v8c-1-1.2-2.5-2.5-4.5-2.5"></path>
					</symbol>
					<symbol id="icon-profile" viewBox="0 0 24 24">
						<rect x="3" y="4" width="18" height="16" rx="2"></rect>
						<circle cx="9" cy="10" r="2.2"></circle>
						<path d="M5.6 16c.8-1.9 2-2.8 3.4-2.8s2.6.9 3.4 2.8"></path>
						<line x1="14" y1="9" x2="18" y2="9"></line>
						<line x1="14" y1="13" x2="18" y2="13"></line>
					</symbol>
				</svg>
				<div class="media-groups-grid">
					{
						externalMediaCategories.map((category) => (
							<article class="media-group-card" data-kind={category.iconKind}>
								<header class="media-group-head">
									<span class="media-category-icon" aria-hidden="true">
										<svg viewBox="0 0 24 24">
											<use href={`#icon-${category.iconKind}`}></use>
										</svg>
									</span>
									<h3>{category.title}</h3>
								</header>
								<ul class="media-link-list">
									{
										category.links.map((item) => {
											const previewSrc = item.previewImage || getYouTubeThumbnailFromUrl(item.href);
											return (
												<li class:list={['media-link-item', previewSrc && 'has-preview']} data-kind={item.kind}>
													<span class="media-kind-chip" data-kind={item.kind}>
														{mediaKindLabels[item.kind]}
													</span>
													{
														previewSrc && (
															<a class="media-preview" href={item.href} target="_blank" rel="noreferrer">
																<img
																	src={previewSrc}
																	alt={`${item.title} preview`}
																	loading="lazy"
																	decoding="async"
																/>
															</a>
														)
													}
													<div class="media-link-content">
														<p class="media-link-meta">
															<span class="media-source">Fuente: {item.source}</span>
														</p>
														<a class="media-link-title" href={item.href} target="_blank" rel="noreferrer">
															{item.title}
														</a>
														<p class="media-link-description">{item.description}</p>
													</div>
												</li>
											);
										})
									}
								</ul>
							</article>
						))
					}
				</div>
			</section>

		<section id="contact" class="contact">
			<h2>¿Te gustaría colaborar?</h2>
			<p>Contáctame a continuación.</p>

			<div class="contact-layout">
				<div class="map-wrap">
					<iframe
						title="Mapa de West Lafayette"
						src="https://maps.google.com/maps?q=west%20lafayette%2C%20indiana&t=m&z=13&output=embed&iwloc=near"
						loading="lazy"
						referrerpolicy="strict-origin-when-cross-origin"
					></iframe>
				</div>

					<div class="contact-grid">
						<article>
							<h3>Ubicación</h3>
							<p>West Lafayette, Indiana</p>
						</article>
						<article>
							<h3>Correo</h3>
							<p><a href="mailto:moacirmabe@gmail.com">moacirmabe@gmail.com</a></p>
							<p><a href="mailto:mfonsec@purdue.edu">mfonsec@purdue.edu</a></p>
						</article>
					<article>
						<h3>Redes</h3>
						<div class="social-links">
							<a
								class="social-link"
								href="https://www.instagram.com/astro_moa/"
								target="_blank"
								rel="noreferrer"
								aria-label="Instagram"
							>
								<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
									<rect x="4.2" y="4.2" width="15.6" height="15.6" rx="4.6" fill="none" stroke="currentColor" stroke-width="1.8"></rect>
									<circle cx="12" cy="12" r="3.6" fill="none" stroke="currentColor" stroke-width="1.8"></circle>
									<circle cx="17.1" cy="6.9" r="1.1" fill="currentColor"></circle>
								</svg>
							</a>
							<a
								class="social-link"
								href="https://www.facebook.com/Moacirmabe"
								target="_blank"
								rel="noreferrer"
								aria-label="Facebook"
							>
								<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
									<path
										fill="currentColor"
										d="M14.4 8.2H17V4h-2.7C11.4 4 9.6 5.9 9.6 8.9v2H7v3.8h2.6V20h3.7v-5.3h2.9l.5-3.8h-3.4V9.2c0-.6.5-1 1.1-1z"
									></path>
								</svg>
							</a>
							<a
								class="social-link social-link--whatsapp"
								href="https://wa.me/50684365396"
								target="_blank"
								rel="noreferrer"
								aria-label="WhatsApp Costa Rica"
							>
								<svg viewBox="0 0 16 16" aria-hidden="true" focusable="false">
									<path
										fill="currentColor"
										d="M13.601 2.326A7.85 7.85 0 0 0 7.994 0C3.627 0 .068 3.558.064 7.926c0 1.399.366 2.76 1.057 3.965L0 16l4.204-1.102a7.9 7.9 0 0 0 3.79.965h.004c4.368 0 7.926-3.558 7.93-7.93A7.9 7.9 0 0 0 13.6 2.326zM7.994 14.521a6.6 6.6 0 0 1-3.356-.92l-.24-.144-2.494.654.666-2.433-.156-.251a6.56 6.56 0 0 1-1.007-3.505c0-3.626 2.957-6.584 6.591-6.584a6.56 6.56 0 0 1 4.66 1.931 6.56 6.56 0 0 1 1.928 4.66c-.004 3.639-2.961 6.592-6.592 6.592m3.615-4.934c-.197-.099-1.17-.578-1.353-.646-.182-.065-.315-.099-.445.099-.133.197-.513.646-.627.775-.114.133-.232.148-.43.05-.197-.1-.836-.308-1.592-.985-.59-.525-.985-1.175-1.103-1.372-.114-.198-.011-.304.088-.403.087-.088.197-.232.296-.346.1-.114.133-.198.198-.33.065-.134.034-.248-.015-.347-.05-.099-.445-1.076-.612-1.47-.16-.389-.323-.335-.445-.34-.114-.007-.247-.007-.38-.007a.73.73 0 0 0-.529.247c-.182.198-.691.677-.691 1.654s.71 1.916.81 2.049c.098.133 1.394 2.132 3.383 2.992.47.205.84.326 1.129.418.475.152.904.129 1.246.08.38-.058 1.171-.48 1.338-.943.164-.464.164-.86.114-.943-.049-.084-.182-.133-.38-.232"
									></path>
								</svg>
							</a>
						</div>
					</article>
					</div>
				</div>
			</section>

			<footer class="site-footer">
				<p>© Moacir Fonseca Becker</p>				
			</footer>

				<div class="image-lightbox" data-image-lightbox aria-hidden="true" role="dialog" aria-modal="true">
				<button
					type="button"
					class="lightbox-close"
					data-lightbox-close
					aria-label="Cerrar imagen ampliada"
				>
					×
				</button>
				<button
					type="button"
					class="lightbox-control lightbox-control--prev"
					data-lightbox-prev
					aria-label="Imagen ampliada anterior"
				>
					‹
				</button>
				<button
					type="button"
					class="lightbox-control lightbox-control--next"
					data-lightbox-next
					aria-label="Imagen ampliada siguiente"
				>
					›
				</button>
				<figure class="lightbox-figure">
					<img data-lightbox-image src="" alt="" />
					<div class="lightbox-video-wrap" data-lightbox-video-wrap hidden>
						<iframe
							data-lightbox-video
							src=""
							title="Video del proyecto ampliado"
							loading="lazy"
							allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
							referrerpolicy="strict-origin-when-cross-origin"
							allowfullscreen
						></iframe>
					</div>
					<figcaption data-lightbox-caption></figcaption>
				</figure>
			</div>
		</main>
	</Layout>

	<script is:inline>
		(() => {
			const themeStorageKey = 'site-theme';
			const root = document.documentElement;
			const themeToggle = document.querySelector('[data-theme-toggle]');
			const darkSchemeMedia =
				window.matchMedia && typeof window.matchMedia === 'function'
					? window.matchMedia('(prefers-color-scheme: dark)')
					: null;

			const getThemeFromStorage = () => {
				try {
					const storedTheme = localStorage.getItem(themeStorageKey);
					return storedTheme === 'light' || storedTheme === 'dark' ? storedTheme : null;
				} catch {
					return null;
				}
			};

			const resolveTheme = () => {
				const storedTheme = getThemeFromStorage();
				if (storedTheme) {
					return storedTheme;
				}
				if (darkSchemeMedia) {
					return darkSchemeMedia.matches ? 'dark' : 'light';
				}
				return root.dataset.theme === 'light' ? 'light' : 'dark';
			};

			const updateThemeToggleLabel = (theme) => {
				if (!(themeToggle instanceof HTMLButtonElement)) {
					return;
				}
				const nextTheme = theme === 'light' ? 'dark' : 'light';
				const nextThemeLabel = nextTheme === 'light' ? 'claro' : 'oscuro';
				const label = `Cambiar a modo ${nextThemeLabel}`;
				themeToggle.setAttribute('aria-label', label);
				themeToggle.setAttribute('aria-pressed', theme === 'dark' ? 'true' : 'false');
				themeToggle.setAttribute('data-theme-cue', label);
				themeToggle.setAttribute('title', label);
			};

			const applyTheme = (theme, persist = false) => {
				const normalizedTheme = theme === 'light' ? 'light' : 'dark';
				root.dataset.theme = normalizedTheme;
				updateThemeToggleLabel(normalizedTheme);

				if (!persist) {
					return;
				}

				try {
					localStorage.setItem(themeStorageKey, normalizedTheme);
				} catch {
					// localStorage may be unavailable in private or restricted contexts.
				}
			};

			applyTheme(resolveTheme());

			if (themeToggle instanceof HTMLButtonElement) {
				themeToggle.addEventListener('click', () => {
					const currentTheme = root.dataset.theme === 'light' ? 'light' : 'dark';
					const nextTheme = currentTheme === 'light' ? 'dark' : 'light';
					applyTheme(nextTheme, true);
				});
			}

			const syncWithSystemTheme = (event) => {
				if (getThemeFromStorage()) {
					return;
				}
				applyTheme(event.matches ? 'dark' : 'light');
			};

			if (darkSchemeMedia) {
				if (typeof darkSchemeMedia.addEventListener === 'function') {
					darkSchemeMedia.addEventListener('change', syncWithSystemTheme);
				} else if (typeof darkSchemeMedia.addListener === 'function') {
					darkSchemeMedia.addListener(syncWithSystemTheme);
				}
			}

			const buttons = Array.from(document.querySelectorAll('.filter-button'));
			const cards = Array.from(document.querySelectorAll('.project-card'));
			const countTarget = document.querySelector('[data-project-count]');
			const hasProjectFilters = buttons.length > 0 && cards.length > 0 && Boolean(countTarget);

			const applyFilter = (filter) => {
				if (!hasProjectFilters || !countTarget) {
					return;
				}

				let visibleCount = 0;
				cards.forEach((card) => {
					const era = card.getAttribute('data-era');
					const hasVideo = card.getAttribute('data-has-video');
					let visible = true;
					if (filter === 'recent') {
						visible = era === 'recent';
					} else if (filter === 'legacy') {
						visible = era === 'legacy';
					} else if (filter === 'video') {
						visible = hasVideo === 'true';
					}

					card.hidden = !visible;
					card.classList.toggle('is-hidden', !visible);
					if (visible) {
						visibleCount += 1;
					}
				});

				const label = visibleCount === 1 ? 'proyecto' : 'proyectos';
				countTarget.textContent = `Mostrando ${visibleCount} ${label}`;
			};

			if (hasProjectFilters) {
				buttons.forEach((button) => {
					button.addEventListener('click', () => {
						const filter = button.getAttribute('data-filter') || 'all';
						buttons.forEach((other) => other.classList.remove('is-active'));
						button.classList.add('is-active');
						applyFilter(filter);
					});
				});
			}

				const carousels = Array.from(document.querySelectorAll('[data-carousel]'));
				carousels.forEach((carousel) => {
					const slides = Array.from(carousel.querySelectorAll('[data-carousel-slide]'));
					const dots = Array.from(carousel.querySelectorAll('[data-carousel-dot]'));
					const prev = carousel.querySelector('[data-carousel-prev]');
				const next = carousel.querySelector('[data-carousel-next]');

				if (slides.length <= 1) {
					carousel.classList.add('is-single');
					return;
				}

				let currentIndex = 0;
				const render = () => {
					slides.forEach((slide, index) => {
						const isActive = index === currentIndex;
						slide.classList.toggle('is-active', isActive);
						slide.setAttribute('aria-hidden', isActive ? 'false' : 'true');
					});
					dots.forEach((dot, index) => {
						const isActive = index === currentIndex;
						dot.classList.toggle('is-active', isActive);
						dot.setAttribute('aria-selected', isActive ? 'true' : 'false');
					});
				};

				prev?.addEventListener('click', () => {
					currentIndex = (currentIndex - 1 + slides.length) % slides.length;
					render();
				});

				next?.addEventListener('click', () => {
					currentIndex = (currentIndex + 1) % slides.length;
					render();
				});

				dots.forEach((dot, index) => {
					dot.addEventListener('click', () => {
						currentIndex = index;
						render();
					});
				});

					render();
				});

				const lightbox = document.querySelector('[data-image-lightbox]');
				const lightboxImage = document.querySelector('[data-lightbox-image]');
				const lightboxVideoWrap = document.querySelector('[data-lightbox-video-wrap]');
				const lightboxVideo = document.querySelector('[data-lightbox-video]');
				const lightboxCaption = document.querySelector('[data-lightbox-caption]');
				const lightboxClose = document.querySelector('[data-lightbox-close]');
				const lightboxPrev = document.querySelector('[data-lightbox-prev]');
				const lightboxNext = document.querySelector('[data-lightbox-next]');
				const galleryImages = Array.from(document.querySelectorAll('.gallery-image'));
				const galleryVideoTriggers = Array.from(document.querySelectorAll('[data-gallery-video-open]'));
				let lightboxSlides = [];
				let lightboxIndex = 0;

				const getCarouselSlides = (carousel) => {
					if (!carousel) {
						return [];
					}
					return Array.from(carousel.querySelectorAll('[data-carousel-slide]'))
						.map((slide) => ({
							node: slide,
							kind: slide.getAttribute('data-lightbox-kind') === 'video' ? 'video' : 'image',
							src: slide.getAttribute('data-lightbox-src') || '',
							alt: slide.getAttribute('data-lightbox-alt') || '',
						}))
						.filter((entry) => entry.src.length > 0);
				};

				const renderLightbox = () => {
					if (!lightbox || !lightboxImage || !lightboxCaption) {
						return;
					}
					const currentSlide = lightboxSlides[lightboxIndex];
					if (!currentSlide) {
						return;
					}

					const isVideo = currentSlide.kind === 'video';
					if (isVideo) {
						if (!lightboxVideoWrap || !lightboxVideo) {
							return;
						}
						lightboxImage.setAttribute('hidden', '');
						lightboxImage.setAttribute('src', '');
						lightboxImage.setAttribute('alt', '');
						lightboxVideoWrap.removeAttribute('hidden');
						lightboxVideo.setAttribute('src', currentSlide.src);
						lightboxVideo.setAttribute('title', currentSlide.alt || 'Video del proyecto ampliado');
					} else {
						lightboxVideo?.setAttribute('src', '');
						lightboxVideoWrap?.setAttribute('hidden', '');
						lightboxImage.removeAttribute('hidden');
						lightboxImage.setAttribute('src', currentSlide.src);
						lightboxImage.setAttribute('alt', currentSlide.alt || 'Expanded project image');
					}

					lightboxCaption.textContent = currentSlide.alt || '';
					const hasMultiple = lightboxSlides.length > 1;
					lightboxPrev?.toggleAttribute('hidden', !hasMultiple);
					lightboxNext?.toggleAttribute('hidden', !hasMultiple);
				};

				const openLightbox = (slides, startIndex) => {
					if (!lightbox || !lightboxImage || !lightboxCaption || !Array.isArray(slides) || slides.length === 0) {
						return;
					}
					lightboxSlides = slides
						.map((slide) => ({
							kind: slide.kind === 'video' ? 'video' : 'image',
							src: slide.src || '',
							alt: slide.alt || '',
						}))
						.filter((slide) => slide.src.length > 0);
					if (lightboxSlides.length === 0) {
						return;
					}

					const normalizedStart = Number.isFinite(startIndex) ? startIndex : 0;
					lightboxIndex = ((normalizedStart % lightboxSlides.length) + lightboxSlides.length) % lightboxSlides.length;
					renderLightbox();
					lightbox.classList.add('is-open');
					lightbox.setAttribute('aria-hidden', 'false');
					document.body.classList.add('lightbox-open');
				};

				const stepLightbox = (delta) => {
					if (lightboxSlides.length <= 1) {
						return;
					}
					lightboxIndex = (lightboxIndex + delta + lightboxSlides.length) % lightboxSlides.length;
					renderLightbox();
				};

				const closeLightbox = () => {
					if (!lightbox || !lightboxImage || !lightboxCaption) {
						return;
					}
					lightbox.classList.remove('is-open');
					lightbox.setAttribute('aria-hidden', 'true');
					lightboxImage.removeAttribute('hidden');
					lightboxImage.setAttribute('src', '');
					lightboxImage.setAttribute('alt', '');
					lightboxVideo?.setAttribute('src', '');
					lightboxVideoWrap?.setAttribute('hidden', '');
					lightboxCaption.textContent = '';
					lightboxSlides = [];
					lightboxIndex = 0;
					lightboxPrev?.setAttribute('hidden', '');
					lightboxNext?.setAttribute('hidden', '');
					document.body.classList.remove('lightbox-open');
				};

				galleryImages.forEach((image) => {
					image.addEventListener('click', () => {
						const carousel = image.closest('[data-carousel]');
						const slide = image.closest('[data-carousel-slide]');
						const slides = getCarouselSlides(carousel);
						const index = Math.max(
							0,
							slides.findIndex((entry) => entry.node === slide),
						);
						openLightbox(slides, index);
					});
				});

				galleryVideoTriggers.forEach((trigger) => {
					const openFromTrigger = () => {
						const carousel = trigger.closest('[data-carousel]');
						const slide = trigger.closest('[data-carousel-slide]');
						const slides = getCarouselSlides(carousel);
						const index = Math.max(
							0,
							slides.findIndex((entry) => entry.node === slide),
						);
						openLightbox(slides, index);
					};

					trigger.addEventListener('click', (event) => {
						event.preventDefault();
						openFromTrigger();
					});

					trigger.addEventListener('keydown', (event) => {
						if (event.key === 'Enter' || event.key === ' ') {
							event.preventDefault();
							openFromTrigger();
						}
					});
				});

				lightboxClose?.addEventListener('click', closeLightbox);
				lightboxPrev?.addEventListener('click', () => {
					stepLightbox(-1);
				});
				lightboxNext?.addEventListener('click', () => {
					stepLightbox(1);
				});
				lightbox?.addEventListener('click', (event) => {
					if (event.target === lightbox) {
						closeLightbox();
					}
				});

				document.addEventListener('keydown', (event) => {
					if (event.key === 'Escape') {
						closeLightbox();
						return;
					}
					if (!lightbox?.classList.contains('is-open')) {
						return;
					}
					if (event.key === 'ArrowLeft') {
						stepLightbox(-1);
					}
					if (event.key === 'ArrowRight') {
						stepLightbox(1);
					}
				});

				const skillsRoot = document.querySelector('[data-skills-chart]');
				const skillsDataNode = document.getElementById('skills-area-data');

				if (skillsRoot && skillsDataNode) {
					let parsedSkillAreas = [];
					try {
						parsedSkillAreas = JSON.parse(skillsDataNode.textContent || '[]');
					} catch {
						parsedSkillAreas = [];
					}

					if (Array.isArray(parsedSkillAreas) && parsedSkillAreas.length > 0) {
						const normalizedAreas = parsedSkillAreas
							.filter((area) => area && typeof area.key === 'string' && Array.isArray(area.skills))
							.map((area) => {
								const normalizedCount = Number(area.count);
								return {
									...area,
									count: Number.isFinite(normalizedCount) ? normalizedCount : area.skills.length,
								};
							});

						const areaByKey = new Map(normalizedAreas.map((area) => [area.key, area]));
						const legendButtons = Array.from(skillsRoot.querySelectorAll('.legend-button'));
						const titleTarget = skillsRoot.querySelector('[data-skill-active-title]');
						const descriptionTarget = skillsRoot.querySelector('[data-skill-active-description]');
						const listTarget = skillsRoot.querySelector('[data-skill-active-list]');
						const sunburst = skillsRoot.querySelector('[data-skills-sunburst]');
						const svgNs = 'http://www.w3.org/2000/svg';
						const chartCenter = 170;
						const outerInnerRadius = 90;
						const outerOuterRadius = 166;
						const innerInnerRadius = 24;
						const innerOuterRadius = 84;
						const totalSkillCount = normalizedAreas.reduce(
							(sum, area) => sum + Math.max(1, Number(area.count) || area.skills.length),
							0,
						);

						const clamp = (value, min, max) => Math.min(max, Math.max(min, value));

						const polarToCartesian = (cx, cy, radius, angleInDegrees) => {
							const angleInRadians = ((angleInDegrees - 90) * Math.PI) / 180;
							return {
								x: cx + radius * Math.cos(angleInRadians),
								y: cy + radius * Math.sin(angleInRadians),
							};
						};

						const getUprightRadialRotation = (angleInDegrees) => {
							const normalized = ((angleInDegrees % 360) + 360) % 360;
							let rotation = normalized + 90;
							if (rotation > 90 && rotation < 270) {
								rotation += 180;
							}
							return ((rotation % 360) + 360) % 360;
						};

						const getRingSectorPath = (cx, cy, innerRadius, outerRadius, startAngle, endAngle) => {
							const outerStart = polarToCartesian(cx, cy, outerRadius, startAngle);
							const outerEnd = polarToCartesian(cx, cy, outerRadius, endAngle);
							const innerEnd = polarToCartesian(cx, cy, innerRadius, endAngle);
							const innerStart = polarToCartesian(cx, cy, innerRadius, startAngle);
							const largeArcFlag = endAngle - startAngle > 180 ? 1 : 0;

							return `M ${outerStart.x.toFixed(3)} ${outerStart.y.toFixed(3)} A ${outerRadius} ${outerRadius} 0 ${largeArcFlag} 1 ${outerEnd.x.toFixed(3)} ${outerEnd.y.toFixed(3)} L ${innerEnd.x.toFixed(3)} ${innerEnd.y.toFixed(3)} A ${innerRadius} ${innerRadius} 0 ${largeArcFlag} 0 ${innerStart.x.toFixed(3)} ${innerStart.y.toFixed(3)} Z`;
						};

						const getShortSkillLabel = (skill) => {
							if (skill.length <= 10) {
								return skill;
							}
							const words = skill.split(' ').filter(Boolean);
							if (words.length === 2 && words[0].length <= 6 && words[1].length <= 6) {
								return `${words[0]} ${words[1]}`;
							}
							return `${skill.slice(0, 8)}…`;
						};

						const buildAreaLayout = (selectedKey) => {
							const selectedArea = areaByKey.get(selectedKey) || normalizedAreas[0];
							if (!selectedArea) {
								return [];
							}

							const selectedCount = Math.max(1, Number(selectedArea.count) || selectedArea.skills.length);
							const selectedBaseSweep = totalSkillCount > 0 ? (selectedCount / totalSkillCount) * 360 : 0;
							const selectedSweep = clamp(selectedBaseSweep + 88, 164, 208);
							const remainingCount = Math.max(0, totalSkillCount - selectedCount);
							const remainingSweep = 360 - selectedSweep;

							let angle = -90;
							return normalizedAreas.map((area, index) => {
								const count = Math.max(1, Number(area.count) || area.skills.length);
								const isSelected = area.key === selectedArea.key;
								const sweep =
									isSelected || remainingCount === 0
										? selectedSweep
										: (count / remainingCount) * remainingSweep;
								const start = angle;
								const end = index === normalizedAreas.length - 1 ? 270 : angle + sweep;
								angle = end;

								return {
									...area,
									count,
									isSelected,
									startAngle: start,
									endAngle: end,
									sweep: end - start,
									midAngle: start + (end - start) / 2,
								};
							});
						};

							const drawSunburst = (layout, selectedKey) => {
								if (!(sunburst instanceof SVGSVGElement)) {
									return;
								}
								sunburst.innerHTML = '';

								const selectedArea = layout.find((area) => area.key === selectedKey);
								const selectedSkills = Array.isArray(selectedArea?.skills) ? selectedArea.skills : [];
								let hoverSliceChip = null;

								const clearHoverSliceLabel = () => {
									if (hoverSliceChip) {
										hoverSliceChip.remove();
										hoverSliceChip = null;
									}
								};

								const showHoverSliceLabel = (labelText, angle, radius) => {
									clearHoverSliceLabel();
									const hoverPoint = polarToCartesian(chartCenter, chartCenter, radius, angle);
									const chip = document.createElementNS(svgNs, 'g');
									chip.setAttribute('class', 'sunburst-hover-chip');
									const label = document.createElementNS(svgNs, 'text');
									label.setAttribute('class', 'sunburst-hover-label');
									label.setAttribute('x', hoverPoint.x.toFixed(2));
									label.setAttribute('y', hoverPoint.y.toFixed(2));
									label.setAttribute('text-anchor', 'middle');
									label.setAttribute('dominant-baseline', 'middle');
									label.textContent = labelText;
									chip.append(label);
									sunburst.append(chip);

									const textBox = label.getBBox();
									const padX = 7;
									const padY = 4;
									const background = document.createElementNS(svgNs, 'rect');
									background.setAttribute('class', 'sunburst-hover-chip-bg');
									background.setAttribute('x', (textBox.x - padX).toFixed(2));
									background.setAttribute('y', (textBox.y - padY).toFixed(2));
									background.setAttribute('width', (textBox.width + padX * 2).toFixed(2));
									background.setAttribute('height', (textBox.height + padY * 2).toFixed(2));
									background.setAttribute('rx', '8');
									background.setAttribute('ry', '8');
									chip.insertBefore(background, label);
									hoverSliceChip = chip;
								};

							layout.forEach((area) => {
								const path = document.createElementNS(svgNs, 'path');
								path.setAttribute('class', `sunburst-parent${area.isSelected ? ' is-active' : ''}`);
								path.setAttribute(
									'd',
									getRingSectorPath(
										chartCenter,
										chartCenter,
										outerInnerRadius,
										outerOuterRadius,
										area.startAngle,
										area.endAngle,
									),
								);
								path.setAttribute('fill', area.color || '#62b8ff');
								path.setAttribute('data-skill-key', area.key);
								path.setAttribute('aria-label', `${area.label}: ${area.count} skills`);
								path.style.transformOrigin = `${chartCenter}px ${chartCenter}px`;

								if (area.isSelected) {
									const radians = ((area.midAngle - 90) * Math.PI) / 180;
									const offset = 8;
									const x = Math.cos(radians) * offset;
									const y = Math.sin(radians) * offset;
									path.style.transform = `translate(${x.toFixed(2)}px, ${y.toFixed(2)}px)`;
								}

									path.addEventListener('click', () => {
										renderArea(area.key);
										if (typeof path.blur === 'function') {
											path.blur();
										}
									});
									path.addEventListener('pointerenter', () =>
										showHoverSliceLabel(area.label, area.midAngle, outerOuterRadius + 14),
									);
									path.addEventListener('pointerleave', clearHoverSliceLabel);

									const tooltip = document.createElementNS(svgNs, 'title');
									tooltip.textContent = `${area.label}: ${area.count} skills`;
								path.append(tooltip);
								sunburst.append(path);

									if (area.sweep >= 18) {
										const labelPosition = polarToCartesian(
											chartCenter,
											chartCenter,
											(outerInnerRadius + outerOuterRadius) / 2 + (area.isSelected ? 5 : 0),
											area.midAngle,
										);
										const labelRotation = getUprightRadialRotation(area.midAngle);
										const label = document.createElementNS(svgNs, 'text');
										label.setAttribute('class', 'sunburst-parent-label');
										label.setAttribute('x', labelPosition.x.toFixed(2));
										label.setAttribute('y', labelPosition.y.toFixed(2));
										label.setAttribute('text-anchor', 'middle');
										label.setAttribute('dominant-baseline', 'middle');
										label.setAttribute(
											'transform',
											`rotate(${labelRotation.toFixed(2)} ${labelPosition.x.toFixed(2)} ${labelPosition.y.toFixed(2)})`,
										);
										label.textContent = area.shortLabel || area.label;
										sunburst.append(label);
									}
							});

							if (selectedArea && selectedSkills.length > 0) {
								const skillSweep = selectedArea.sweep / selectedSkills.length;
								selectedSkills.forEach((skill, index) => {
									const skillStart = selectedArea.startAngle + skillSweep * index;
									const skillEnd = skillStart + skillSweep;
									const skillMid = skillStart + skillSweep / 2;

									const skillPath = document.createElementNS(svgNs, 'path');
									skillPath.setAttribute('class', 'sunburst-child');
									skillPath.setAttribute(
										'd',
										getRingSectorPath(
											chartCenter,
											chartCenter,
											innerInnerRadius,
											innerOuterRadius,
											skillStart,
											skillEnd,
										),
									);
									skillPath.setAttribute('fill', selectedArea.color || '#62b8ff');
									skillPath.setAttribute('fill-opacity', (0.72 + ((index % 3) * 0.1)).toFixed(2));

										const skillTooltip = document.createElementNS(svgNs, 'title');
										skillTooltip.textContent = skill;
										skillPath.append(skillTooltip);
										sunburst.append(skillPath);
										skillPath.addEventListener('pointerenter', () =>
											showHoverSliceLabel(skill, skillMid, innerOuterRadius + 15),
										);
										skillPath.addEventListener('pointerleave', clearHoverSliceLabel);

										if (skillSweep >= 12) {
											const skillLabelPoint = polarToCartesian(
												chartCenter,
											chartCenter,
											(innerInnerRadius + innerOuterRadius) / 2,
											skillMid,
										);
										const skillLabelRotation = getUprightRadialRotation(skillMid);
										const skillLabel = document.createElementNS(svgNs, 'text');
										skillLabel.setAttribute('class', 'sunburst-child-label');
										skillLabel.setAttribute('x', skillLabelPoint.x.toFixed(2));
										skillLabel.setAttribute('y', skillLabelPoint.y.toFixed(2));
										skillLabel.setAttribute('text-anchor', 'middle');
										skillLabel.setAttribute('dominant-baseline', 'middle');
										skillLabel.setAttribute(
											'transform',
											`rotate(${skillLabelRotation.toFixed(2)} ${skillLabelPoint.x.toFixed(2)} ${skillLabelPoint.y.toFixed(2)})`,
										);
											skillLabel.textContent = getShortSkillLabel(skill);
											sunburst.append(skillLabel);
										}
									});
								}

							const core = document.createElementNS(svgNs, 'circle');
							core.setAttribute('class', 'sunburst-core');
							core.setAttribute('cx', String(chartCenter));
							core.setAttribute('cy', String(chartCenter));
							core.setAttribute('r', String(innerInnerRadius - 2));
							sunburst.append(core);
						};

						const renderArea = (key) => {
							const area = areaByKey.get(key);
							if (!area || !titleTarget || !descriptionTarget || !listTarget) {
								return;
							}

							titleTarget.textContent = `${area.label} (${area.count})`;
							descriptionTarget.textContent = area.description;

							listTarget.innerHTML = '';
							area.skills.forEach((skill) => {
								const item = document.createElement('li');
								item.textContent = skill;
								listTarget.append(item);
							});

							const layout = buildAreaLayout(key);
							drawSunburst(layout, key);

							legendButtons.forEach((button) => {
								const isActive = button.getAttribute('data-skill-key') === key;
								button.classList.toggle('is-active', isActive);
								button.setAttribute('aria-pressed', isActive ? 'true' : 'false');
							});
						};

						legendButtons.forEach((button) => {
							const key = button.getAttribute('data-skill-key');
							button.addEventListener('click', () => {
								if (key) {
									renderArea(key);
								}
								if (typeof button.blur === 'function') {
									button.blur();
								}
							});
						});

						const firstKey = normalizedAreas[0]?.key;
						if (firstKey) {
							renderArea(firstKey);
						}
					}
				}

				applyFilter('all');
			})();
		</script>

<style>
	:global(*) {
		box-sizing: border-box;
	}

	:global(body) {
		--bg: #000105;
		--bg-soft: #020916;
		--surface: rgba(4, 12, 30, 0.82);
		--surface-2: rgba(6, 18, 43, 0.88);
		--card: rgba(9, 28, 67, 0.74);
		--line: rgba(114, 176, 255, 0.26);
		--line-strong: rgba(145, 205, 255, 0.56);
		--text: #edf5ff;
		--text-soft: #acc7e8;
		--accent: #58b2ff;
		--accent-2: #8cd7ff;
		background: #000000;
		color: var(--text);
		color-scheme: dark;
		font-family: 'Avenir Next', 'Trebuchet MS', 'Segoe UI', sans-serif;
		line-height: 1.55;
		margin: 0;
		min-height: 100vh;
		transition: background-color 0.25s ease, color 0.25s ease;
	}

	:global(html[data-theme='light'] body) {
		--bg: #ffffff;
		--bg-soft: #f7fbff;
		--surface: rgba(255, 255, 255, 0.88);
		--surface-2: rgba(248, 252, 255, 0.92);
		--card: rgba(255, 255, 255, 0.88);
		--line: rgba(40, 95, 161, 0.2);
		--line-strong: rgba(40, 95, 161, 0.42);
		--text: #12263f;
		--text-soft: #4f6684;
		--accent: #2168bf;
		--accent-2: #1458a7;
		background: #ffffff;
		background-attachment: fixed;
		color: var(--text);
		color-scheme: light;
	}

	:global(body.lightbox-open) {
		overflow: hidden;
	}

	.page {
		margin: 0 auto;
		max-width: 72rem;
		padding: 0 clamp(1.1rem, 4vw, 3.8rem) 3.4rem;
		position: relative;
		width: 100%;
	}

	.page > :not(.aurora):not(.image-lightbox) {
		position: relative;
		z-index: 1;
	}

	.aurora {
		inset: 0;
		overflow: hidden;
		pointer-events: none;
		position: fixed;
		z-index: 0;
	}

	.bg-image-layer {
		background-image: url('/backgrounds/site1-bg.png');
		background-position: center;
		background-repeat: no-repeat;
		background-size: cover;
		filter: brightness(0.4) contrast(1.06) saturate(0.82);
		height: 100vw;
		left: 50%;
		opacity: 0.93;
		position: absolute;
		top: 50%;
		transform: translate(-50%, -50%) rotate(90deg);
		transform-origin: center;
		width: 100vh;
		z-index: 0;
	}

	.aurora::before,
	.aurora::after {
		content: '';
		inset: 0;
		position: absolute;
		z-index: 1;
	}

	.aurora::before {
		animation: starDrift 130s linear infinite;
		background-image:
			radial-gradient(circle at 15% 22%, rgba(255, 255, 255, 0.68) 0 1px, transparent 1.8px),
			radial-gradient(circle at 80% 10%, rgba(160, 212, 255, 0.56) 0 1px, transparent 1.8px),
			radial-gradient(circle at 42% 76%, rgba(178, 223, 255, 0.52) 0 1px, transparent 1.8px);
		background-size: 180px 180px, 240px 240px, 300px 300px;
		opacity: 0.2;
	}

	.aurora::after {
		animation: starDrift 190s linear infinite reverse;
		background-image:
			radial-gradient(circle at 60% 35%, rgba(164, 214, 255, 0.44) 0 1px, transparent 1.7px),
			radial-gradient(circle at 24% 64%, rgba(255, 255, 255, 0.4) 0 1px, transparent 1.7px);
		background-size: 260px 260px, 340px 340px;
		opacity: 0.14;
	}

	.orb {
		border-radius: 999px;
		filter: blur(12px);
		position: absolute;
		z-index: 2;
	}

	.orb-one {
		animation: drift 18s ease-in-out infinite alternate;
		background: radial-gradient(circle, rgba(86, 181, 255, 0.34) 0%, rgba(86, 181, 255, 0) 70%);
		height: min(24rem, 46vw);
		left: 0;
		top: 0;
		width: min(24rem, 46vw);
	}

	.orb-two {
		animation: drift 22s ease-in-out infinite alternate-reverse;
		background: radial-gradient(circle, rgba(64, 223, 255, 0.24) 0%, rgba(64, 223, 255, 0) 70%);
		height: min(26rem, 48vw);
		right: 0;
		top: 35%;
		width: min(26rem, 48vw);
	}

	.orb-three {
		animation: drift 26s ease-in-out infinite alternate;
		background: radial-gradient(circle, rgba(125, 170, 255, 0.24) 0%, rgba(125, 170, 255, 0) 70%);
		bottom: 0;
		height: min(23rem, 44vw);
		left: 30%;
		width: min(23rem, 44vw);
	}

	.site-header {
		align-items: center;
		backdrop-filter: blur(14px);
		background: rgba(3, 10, 25, 0.58);
		border: 0;
		border-radius: 0;
		box-shadow: none;
		display: grid;
		gap: 0.85rem;
		grid-template-columns: auto 1fr auto;
		margin-bottom: 1rem;
		margin-left: calc(50% - 50vw);
		padding: 0.62rem clamp(1.1rem, 4vw, 3.8rem);
		position: sticky;
		top: 0;
		width: 100vw;
		z-index: 40;
	}

	.brand {
		background: transparent !important;
		padding: 0 !important;
	}

	.brand:hover {
		background: transparent !important;
		transform: none !important;
	}

	.brand-name {
		background: linear-gradient(120deg, #8cd7ff 0%, #f2fbff 52%, #58b2ff 100%);
		color: #bdefff;
		display: inline-block;
		font-family: 'Didot', 'Bodoni MT', 'Palatino Linotype', serif;
		font-size: clamp(1rem, 1.75vw, 1.3rem);
		font-weight: 700;
		letter-spacing: 0.04em;
		line-height: 1.05;
		text-wrap: balance;
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.site-header ul {
		display: flex;
		flex-wrap: wrap;
		gap: 0.2rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.site-header a {
		border-radius: 0.3rem;
		color: var(--text);
		display: inline-block;
		font-size: 0.86rem;
		font-weight: 600;
		padding: 0.38rem 0.72rem;
		text-decoration: none;
		transition: background-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
	}

	.site-header a:hover {
		background: rgba(129, 222, 255, 0.24);
		color: #ffffff;
		transform: translateY(-1px);
	}

	.cv-link {
		background: transparent;
		border: 1px solid var(--line-strong);
		box-shadow: none;
		color: #f3fbff !important;
		font-weight: 700;
	}

	.header-actions {
		align-items: center;
		display: flex;
		gap: 0.5rem;
		justify-self: end;
	}

	.theme-toggle {
		align-items: center;
		background: transparent;
		border: 0;
		border-radius: 999px;
		color: var(--text);
		cursor: pointer;
		display: inline-flex;
		height: 2rem;
		justify-content: center;
		padding: 0;
		position: relative;
		transition: color 0.2s ease, transform 0.2s ease;
		width: 2rem;
	}

	.theme-toggle:hover {
		background: transparent;
		color: #ffffff;
		filter: drop-shadow(0 0 8px rgba(124, 210, 255, 0.65));
		transform: translateY(-1px);
	}

	.theme-toggle:focus-visible {
		outline: 2px solid rgba(193, 236, 255, 0.95);
		outline-offset: 2px;
	}

	.theme-toggle::after {
		background: rgba(4, 18, 44, 0.92);
		border: 1px solid rgba(135, 205, 255, 0.52);
		border-radius: 999px;
		bottom: -1.85rem;
		color: #dff4ff;
		content: attr(data-theme-cue);
		font-size: 0.61rem;
		font-weight: 700;
		left: 50%;
		letter-spacing: 0.03em;
		opacity: 0;
		padding: 0.18rem 0.45rem;
		pointer-events: none;
		position: absolute;
		text-transform: uppercase;
		transform: translate(-50%, 4px);
		transition: opacity 0.18s ease, transform 0.18s ease;
		white-space: nowrap;
	}

	.theme-toggle:hover::after,
	.theme-toggle:focus-visible::after {
		opacity: 1;
		transform: translate(-50%, 0);
	}

	.language-toggle {
		align-items: center;
		background: transparent !important;
		border: 0;
		border-radius: 0;
		color: var(--text);
		display: inline-flex;
		font-size: 1.08rem !important;
		font-weight: 800 !important;
		justify-content: center;
		letter-spacing: 0;
		line-height: 1;
		min-width: 0;
		padding: 0 !important;
		position: relative;
		text-decoration: none;
		transition: color 0.2s ease, filter 0.2s ease, transform 0.2s ease;
	}

	.language-toggle:hover {
		background: transparent !important;
		filter: drop-shadow(0 0 8px rgba(124, 210, 255, 0.65));
		transform: translateY(-1px) scale(1.04);
	}

	.language-toggle::after {
		background: rgba(4, 18, 44, 0.92);
		border: 1px solid rgba(135, 205, 255, 0.52);
		border-radius: 999px;
		bottom: -1.85rem;
		color: #dff4ff;
		content: attr(data-language-cue);
		font-size: 0.61rem;
		font-weight: 700;
		left: 50%;
		letter-spacing: 0.03em;
		opacity: 0;
		padding: 0.18rem 0.45rem;
		pointer-events: none;
		position: absolute;
		text-transform: uppercase;
		transform: translate(-50%, 4px);
		transition: opacity 0.18s ease, transform 0.18s ease;
		white-space: nowrap;
	}

	.language-toggle:hover::after,
	.language-toggle:focus-visible::after {
		opacity: 1;
		transform: translate(-50%, 0);
	}

	.theme-icon {
		display: inline-flex;
		position: absolute;
		transition: opacity 0.2s ease, transform 0.2s ease;
	}

	.theme-icon svg {
		fill: none;
		height: 1rem;
		stroke: currentColor;
		stroke-linecap: round;
		stroke-linejoin: round;
		stroke-width: 1.8;
		width: 1rem;
	}

	.theme-icon--sun {
		opacity: 0;
		transform: scale(0.75) rotate(-14deg);
	}

	.theme-icon--moon {
		opacity: 1;
		transform: scale(1) rotate(0deg);
	}

	:global(html[data-theme='light']) .theme-icon--sun {
		opacity: 1;
		transform: scale(1) rotate(0deg);
	}

	:global(html[data-theme='light']) .theme-icon--moon {
		opacity: 0;
		transform: scale(0.75) rotate(14deg);
	}

	.hero,
	.section-shell,
	.contact,
	.site-footer {
		animation: rise 0.55s ease both;
	}

	.hero {
		backdrop-filter: none;
		background: transparent;
		border: 0;
		border-radius: 0;
		box-shadow: none;
		display: grid;
		gap: 1rem;
		margin-bottom: 0;
		padding: 1.1rem 0 1.6rem;
	}

	.hero-main {
		align-items: start;
		display: grid;
		gap: 1.1rem;
		grid-template-columns: minmax(0, 1fr) minmax(14rem, 17rem);
	}

	.hero-photo-wrap {
		align-self: start;
		justify-self: end;
	}

	.hero-photo {
		border: 1px solid rgba(154, 221, 255, 0.38);
		border-radius: 0.7rem;
		box-shadow: 0 12px 24px rgba(3, 11, 28, 0.34);
		display: block;
		height: auto;
		max-width: 100%;
		object-fit: cover;
		width: min(17rem, 30vw);
	}

	.eyebrow {
		color: var(--accent-2);
		display: grid;
		gap: 0.14rem;
		margin: 0;
	}

	.eyebrow-flags {
		font-size: 1.14rem;
		letter-spacing: 0.2em;
		line-height: 1;
	}

	.eyebrow-greetings {
		font-size: 1.02rem;
		font-weight: 700;
		letter-spacing: 0.09em;
		text-transform: uppercase;
	}

	h1 {
		color: #ffffff;
		font-family: 'Iowan Old Style', 'Palatino Linotype', 'Book Antiqua', serif;
		font-size: clamp(2.2rem, 5vw, 3.9rem);
		line-height: 1.04;
		margin: 0;
		text-wrap: balance;
	}

	h2,
	h3 {
		color: #ffffff;
		font-family: 'Iowan Old Style', 'Palatino Linotype', 'Book Antiqua', serif;
		margin-top: 0;
	}

	h2 {
		-webkit-text-fill-color: currentColor;
		display: inline-block;
		font-size: clamp(1.7rem, 3.2vw, 2.75rem);
		letter-spacing: 0.015em;
		line-height: 1.1;
		margin-bottom: 0.5rem;
		transition: color 0.24s ease, text-shadow 0.24s ease, filter 0.24s ease;
	}

	h2:hover {
		background: linear-gradient(120deg, #8fd8ff 0%, #e9f9ff 44%, #67b9ff 100%);
		background-clip: text;
		filter: drop-shadow(0 0 10px rgba(122, 209, 255, 0.45));
		text-shadow: 0 0 18px rgba(102, 188, 255, 0.45), 0 0 30px rgba(58, 143, 234, 0.25);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.hero-copy {
		display: grid;
		gap: 0.75rem;
		max-width: 76ch;
	}

	.hero-copy p,
	.section-shell p {
		margin: 0;
	}

	.thank-you {
		color: var(--accent-2);
		font-weight: 700;
		margin: 0;
	}

	.section-shell,
	.contact {
		backdrop-filter: none;
		background: transparent;
		border: 0;
		border-radius: 0;
		box-shadow: none;
		margin-bottom: 0;
		padding: 1.25rem 0;
	}

	.section-header p {
		color: var(--text-soft);
		margin-top: 0.25rem;
	}

	.portfolio-toolbar {
		display: flex;
		flex-wrap: wrap;
		gap: 0.45rem;
		margin-bottom: 0.4rem;
	}

	.filter-button {
		background: transparent;
		border: 1px solid transparent;
		border-radius: 0.28rem;
		color: #e9f7ff;
		cursor: pointer;
		font-size: 0.82rem;
		font-weight: 700;
		letter-spacing: 0.03em;
		padding: 0.34rem 0.75rem;
		text-transform: uppercase;
		transition: transform 0.2s ease, border-color 0.2s ease, background-color 0.2s ease;
	}

	.filter-button:hover {
		background: rgba(75, 152, 255, 0.2);
		border-color: rgba(150, 229, 255, 0.44);
		transform: none;
	}

	.filter-button.is-active {
		background: rgba(80, 187, 255, 0.2);
		border-color: rgba(150, 229, 255, 0.9);
		color: #ffffff;
	}

	.portfolio-count {
		color: var(--text-soft) !important;
		font-size: 0.9rem;
		margin-bottom: 0.85rem !important;
	}

	.project-grid {
		display: grid;
		gap: 1rem;
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	.leadership-grid {
		display: grid;
		gap: 0.85rem;
		grid-template-columns: repeat(2, minmax(0, 1fr));
	}

	.education-grid {
		display: grid;
		gap: 0.75rem;
		grid-template-columns: repeat(4, minmax(0, 1fr));
	}

	.media-groups-grid {
		display: grid;
		gap: 0.85rem;
		grid-template-columns: repeat(2, minmax(0, 1fr));
	}

	.media-icon-defs {
		height: 0;
		position: absolute;
		width: 0;
	}

	.media-group-card {
		background: rgba(6, 20, 48, 0.58);
		border: 1px solid rgba(114, 176, 255, 0.3);
		border-radius: 0.55rem;
		display: grid;
		gap: 0.62rem;
		padding: 0.72rem 0.74rem;
		transition: border-color 0.2s ease, background-color 0.2s ease;
	}

	.media-group-card:hover {
		background: rgba(10, 28, 64, 0.74);
		border-color: rgba(155, 214, 255, 0.62);
	}

	.media-group-head {
		align-items: center;
		display: flex;
		gap: 0.52rem;
	}

	.media-category-icon {
		align-items: center;
		background: transparent;
		border: 0;
		border-radius: 0;
		color: #bde9ff;
		display: inline-flex;
		height: 1.85rem;
		justify-content: center;
		width: 1.85rem;
	}

	.media-category-icon svg {
		height: 1.42rem;
		width: 1.42rem;
	}

	.media-category-icon svg * {
		fill: none;
		stroke: currentColor;
		stroke-linecap: round;
		stroke-linejoin: round;
		stroke-width: 1.8;
	}

	.media-category-icon svg polygon {
		fill: currentColor;
		stroke: none;
	}

	.media-group-card[data-kind='podcast'] .media-category-icon {
		color: #86dbff;
	}

	.media-group-card[data-kind='news'] .media-category-icon {
		color: #c6e3ff;
	}

	.media-group-card[data-kind='official'] .media-category-icon {
		color: #9dc3ff;
	}

	.media-group-card[data-kind='outreach'] .media-category-icon {
		color: #9ff1e6;
	}

	.media-group-card h3 {
		font-size: 0.97rem;
		line-height: 1.2;
		margin: 0;
	}

	.media-link-list {
		display: grid;
		gap: 0.62rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.media-link-item {
		border-top: 1px solid rgba(114, 176, 255, 0.24);
		display: grid;
		gap: 0.45rem;
		position: relative;
		padding-right: 4.7rem;
		padding-top: 0.62rem;
	}

	.media-link-item.has-preview {
		grid-template-columns: minmax(7.1rem, 8.4rem) 1fr;
	}

	.media-link-item:first-child {
		border-top: 0;
		padding-top: 0;
	}

	.media-link-item:first-child .media-kind-chip {
		top: 0;
	}

	.media-preview {
		border: 1px solid rgba(127, 196, 255, 0.36);
		border-radius: 0.38rem;
		display: block;
		height: fit-content;
		overflow: hidden;
		position: relative;
		text-decoration: none;
	}

	.media-preview img {
		aspect-ratio: 16 / 9;
		display: block;
		height: 100%;
		object-fit: cover;
		transition: transform 0.2s ease;
		width: 100%;
	}

	.media-link-item:hover .media-preview img {
		transform: scale(1.04);
	}

	.media-link-content {
		display: grid;
		gap: 0.24rem;
		padding-top: 0.1rem;
	}

	.media-link-meta {
		align-items: center;
		display: flex;
		flex-wrap: wrap;
		gap: 0.34rem;
		margin: 0 !important;
	}

	.media-kind-chip {
		background: rgba(25, 59, 110, 0.52);
		border: 1px solid rgba(122, 188, 255, 0.36);
		border-radius: 999px;
		color: #cdeeff;
		display: inline-block;
		font-size: 0.56rem;
		font-weight: 700;
		line-height: 1.05;
		letter-spacing: 0.04em;
		padding: 0.14rem 0.38rem;
		position: absolute;
		right: 0;
		top: 0.62rem;
		text-transform: uppercase;
		white-space: nowrap;
		z-index: 2;
	}

	.media-kind-chip[data-kind='podcast'] {
		color: #97e3ff;
	}

	.media-kind-chip[data-kind='video'] {
		color: #90d4ff;
	}

	.media-kind-chip[data-kind='news'] {
		color: #c7e5ff;
	}

	.media-kind-chip[data-kind='official'] {
		color: #adc8ff;
	}

	.media-kind-chip[data-kind='outreach'] {
		color: #a4efe5;
	}

	.media-kind-chip[data-kind='profile'] {
		color: #e2e9ff;
	}

	.media-link-title {
		color: #9be3ff;
		font-size: 0.88rem;
		font-weight: 700;
		line-height: 1.28;
		text-decoration: none;
	}

	.media-link-title:hover {
		color: #d5f4ff;
		text-decoration: underline;
	}

	.media-link-description {
		color: var(--text-soft);
		font-size: 0.79rem;
		line-height: 1.36;
		margin: 0 !important;
	}

	.media-source {
		color: #84bee8;
		font-size: 0.72rem;
		letter-spacing: 0.01em;
		opacity: 0.95;
	}

	.project-card {
		background: rgba(6, 20, 48, 0.62);
		border: 1px solid rgba(114, 176, 255, 0.32);
		border-radius: 0.45rem;
		display: grid;
		gap: 0.58rem;
		padding: 0.7rem;
		position: relative;
		transition: border-color 0.22s ease, background-color 0.22s ease;
	}

	.project-card[hidden] {
		display: none !important;
	}

	.contact-grid article {
		background: transparent;
		border: 0;
		border-radius: 0;
		display: grid;
		gap: 0.72rem;
		padding: 1rem 0;
		position: relative;
		transition: border-color 0.25s ease;
	}

	.leadership-card {
		background: rgba(8, 24, 56, 0.62);
		border: 1px solid rgba(122, 188, 255, 0.32);
		border-radius: 0.5rem;
		display: grid;
		gap: 0.58rem;
		padding: 0.74rem 0.78rem;
		position: relative;
		transition: border-color 0.22s ease, background-color 0.22s ease;
	}

	.education-card {
		background: rgba(8, 24, 56, 0.68);
		border: 1px solid rgba(122, 188, 255, 0.34);
		border-radius: 0.45rem;
		display: grid;
		gap: 0.56rem;
		padding: 0.68rem;
		position: relative;
		transition: border-color 0.22s ease, background-color 0.22s ease;
	}

	.project-card::before {
		display: none;
	}

	.project-card:hover,
	.contact-grid article:hover {
		border-color: var(--line-strong);
	}

	.project-card:hover {
		background: rgba(9, 28, 67, 0.75);
	}

	.leadership-card:hover {
		background: rgba(12, 33, 75, 0.78);
		border-color: rgba(161, 223, 255, 0.72);
	}

	.education-card:hover {
		background: rgba(12, 33, 75, 0.78);
		border-color: rgba(161, 223, 255, 0.72);
	}

	.project-card.is-hidden {
		opacity: 0;
		transform: none;
	}

	.card-header {
		align-items: center;
		display: flex;
		gap: 0.45rem;
		justify-content: space-between;
	}

	.project-card .year {
		background: rgba(83, 194, 255, 0.24);
		border: 1px solid rgba(117, 215, 255, 0.36);
		border-radius: 999px;
		color: #e5f9ff;
		display: inline-block;
		font-size: 0.79rem;
		font-weight: 700;
		margin: 0;
		padding: 0.18rem 0.5rem;
		width: fit-content;
	}

	.video-chip {
		background: rgba(128, 173, 255, 0.26);
		border: 1px solid rgba(145, 187, 255, 0.45);
		border-radius: 999px;
		color: #dcecff;
		font-size: 0.7rem;
		font-weight: 700;
		padding: 0.16rem 0.5rem;
		text-transform: uppercase;
	}

	.project-card h3,
	.leadership-card h3,
	.education-card h3 {
		font-size: 1rem;
		line-height: 1.24;
		margin-bottom: 0;
	}

	.leadership-card .detail-list {
		font-size: 0.88rem;
		gap: 0.32rem;
	}

	.note {
		color: var(--text-soft);
		font-size: 0.89rem;
	}

	.project-gallery-wrap {
		display: grid;
		gap: 0.45rem;
	}

	.gallery-label {
		color: #c8e8ff;
		font-size: 0.78rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		margin: 0;
		text-transform: uppercase;
	}

	.project-carousel {
		align-items: center;
		display: grid;
		gap: 0.45rem;
		grid-template-columns: auto 1fr auto;
	}

	.project-carousel.is-single {
		grid-template-columns: 1fr;
	}

	.carousel-control {
		align-items: center;
		background: transparent;
		border: 1px solid rgba(132, 201, 255, 0.44);
		border-radius: 0.3rem;
		color: #e5f6ff;
		cursor: pointer;
		display: inline-flex;
		font-size: 1.05rem;
		font-weight: 700;
		height: 1.8rem;
		justify-content: center;
		line-height: 1;
		padding: 0;
		transition: transform 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
		width: 1.8rem;
	}

	.carousel-control:hover {
		background: rgba(76, 153, 255, 0.24);
		border-color: rgba(154, 222, 255, 0.8);
		transform: none;
	}

	.project-carousel.is-single .carousel-control,
	.project-carousel.is-single .carousel-dots {
		display: none;
	}

	.project-gallery {
		background: transparent;
		border: 0;
		border-radius: 0;
		min-height: 0;
		overflow: hidden;
		position: relative;
	}

	.gallery-item {
		display: none;
		aspect-ratio: 16 / 9;
		margin: 0;
		padding: 0;
		width: 100%;
	}

	.gallery-item.is-active {
		animation: fadeSlide 0.28s ease;
		display: block;
	}

	.gallery-item img {
		display: block;
		height: 100%;
		object-fit: contain;
		width: 100%;
		transition: transform 0.24s ease;
		cursor: zoom-in;
	}

	.gallery-video-wrap {
		align-items: center;
		background: rgba(1, 8, 22, 0.9);
		border: 1px solid rgba(112, 182, 255, 0.28);
		border-radius: 0;
		cursor: zoom-in;
		display: flex;
		height: 100%;
		justify-content: center;
		overflow: hidden;
		position: relative;
		width: 100%;
	}

	.gallery-video-thumb {
		display: block;
		height: 100%;
		object-fit: contain;
		width: 100%;
	}

	.gallery-video-fallback {
		align-items: center;
		color: #d9f0ff;
		display: flex;
		font-size: 0.8rem;
		font-weight: 700;
		height: 100%;
		justify-content: center;
		padding: 0.4rem;
		text-align: center;
		width: 100%;
	}

	.gallery-video-play {
		background: rgba(1, 11, 27, 0.62);
		border: 2px solid rgba(236, 250, 255, 0.86);
		border-radius: 999px;
		height: 3.05rem;
		left: 50%;
		pointer-events: none;
		position: absolute;
		top: 50%;
		transform: translate(-50%, -50%);
		width: 3.05rem;
	}

	.gallery-video-play::after {
		border-bottom: 0.56rem solid transparent;
		border-left: 0.88rem solid rgba(238, 250, 255, 0.95);
		border-top: 0.56rem solid transparent;
		content: '';
		left: 50%;
		position: absolute;
		top: 50%;
		transform: translate(-40%, -50%);
	}

	.gallery-video-wrap::after {
		background: rgba(3, 16, 40, 0.86);
		border: 1px solid rgba(154, 220, 255, 0.8);
		border-radius: 999px;
		color: #f1fbff;
		content: 'Haz clic para ampliar';
		font-size: 0.74rem;
		font-weight: 700;
		left: 50%;
		letter-spacing: 0.04em;
		opacity: 0;
		padding: 0.3rem 0.62rem;
		position: absolute;
		text-transform: uppercase;
		top: 50%;
		transform: translate(-50%, -50%);
		transition: opacity 0.18s ease, background-color 0.18s ease;
		white-space: nowrap;
	}

	.gallery-video-wrap:hover::after,
	.gallery-video-wrap:focus-visible::after {
		opacity: 1;
	}

	.gallery-video-wrap:focus-visible {
		outline: 2px solid rgba(190, 233, 255, 0.95);
		outline-offset: 2px;
	}

	.gallery-item:hover img {
		transform: scale(1.03);
	}

	.carousel-dots {
		display: flex;
		gap: 0.34rem;
		grid-column: 1 / -1;
		justify-content: center;
	}

	.carousel-dot {
		background: rgba(148, 204, 255, 0.34);
		border: 0;
		border-radius: 999px;
		cursor: pointer;
		height: 0.52rem;
		padding: 0;
		transition: transform 0.18s ease, background-color 0.18s ease;
		width: 0.52rem;
	}

	.carousel-dot.is-active {
		background: rgba(145, 224, 255, 0.95);
		transform: scale(1.15);
	}

	.image-lightbox {
		align-items: center;
		background: rgba(1, 6, 18, 0.88);
		display: flex;
		inset: 0;
		justify-content: center;
		opacity: 0;
		padding: clamp(1rem, 3vw, 2rem);
		pointer-events: none;
		position: fixed;
		transition: opacity 0.2s ease;
		z-index: 120;
	}

	.image-lightbox.is-open {
		opacity: 1;
		pointer-events: auto;
	}

	.lightbox-close {
		background: rgba(15, 40, 84, 0.8);
		border: 1px solid rgba(153, 220, 255, 0.7);
		border-radius: 0.35rem;
		color: #f0fbff;
		cursor: pointer;
		font-size: 1.5rem;
		height: 2.2rem;
		line-height: 1;
		position: absolute;
		right: clamp(0.8rem, 2vw, 1.4rem);
		top: clamp(0.8rem, 2vw, 1.4rem);
		width: 2.2rem;
	}

	.lightbox-control {
		align-items: center;
		background: rgba(5, 19, 44, 0.88);
		border: 1px solid rgba(149, 215, 255, 0.78);
		border-radius: 999px;
		color: #eef9ff;
		cursor: pointer;
		display: inline-flex;
		font-size: 1.9rem;
		font-weight: 700;
		height: 2.8rem;
		justify-content: center;
		line-height: 1;
		padding: 0;
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		transition: background-color 0.2s ease, border-color 0.2s ease;
		width: 2.8rem;
		z-index: 3;
	}

	.lightbox-control:hover {
		background: rgba(33, 89, 168, 0.78);
		border-color: rgba(190, 233, 255, 0.95);
	}

	.lightbox-control[hidden] {
		display: none;
	}

	.lightbox-control--prev {
		left: clamp(0.65rem, 2vw, 1.35rem);
	}

	.lightbox-control--next {
		right: clamp(0.65rem, 2vw, 1.35rem);
	}

	.lightbox-figure {
		display: grid;
		gap: 0.5rem;
		margin: 0;
		max-height: 90vh;
		max-width: min(92vw, 64rem);
	}

	.lightbox-figure img {
		display: block;
		max-height: 82vh;
		max-width: 100%;
		object-fit: contain;
		width: auto;
	}

	.lightbox-video-wrap {
		width: min(92vw, 64rem);
	}

	.lightbox-video-wrap iframe {
		aspect-ratio: 16 / 9;
		border: 0;
		display: block;
		max-height: 82vh;
		width: 100%;
	}

	.lightbox-figure figcaption {
		color: #b9ddff;
		font-size: 0.9rem;
		margin: 0;
		text-align: center;
	}

	@media (max-width: 640px) {
		.lightbox-control {
			font-size: 1.6rem;
			height: 2.45rem;
			width: 2.45rem;
		}
	}

	.project-contrib {
		display: inline-flex;
		position: relative;
		width: fit-content;
		z-index: 6;
	}

	.project-contrib-trigger {
		background: none;
		border: 0;
		color: var(--accent-2);
		cursor: pointer;
		font-size: 0.8rem;
		font-weight: 700;
		line-height: 1;
		padding: 0;
		text-decoration: underline;
		text-underline-offset: 0.12rem;
	}

	.project-contrib-popover {
		background: #051a3f;
		border: 1px solid #8acbff;
		border-radius: 0.46rem;
		box-shadow: 0 10px 20px rgba(1, 8, 22, 0.46);
		left: 0;
		max-width: min(24rem, calc(100vw - 2rem));
		opacity: 0;
		padding: 0.52rem 0.6rem 0.56rem;
		pointer-events: none;
		position: absolute;
		top: calc(100% + 0.36rem);
		transform: translateY(5px);
		transition: opacity 0.18s ease, transform 0.18s ease;
		visibility: hidden;
		width: min(24rem, calc(100vw - 2rem));
		z-index: 12;
	}

	.project-contrib:hover .project-contrib-popover,
	.project-contrib:focus-within .project-contrib-popover {
		opacity: 1;
		pointer-events: auto;
		transform: translateY(0);
		visibility: visible;
	}

	.project-contrib-title {
		color: #dff3ff;
		font-size: 0.74rem;
		font-weight: 700;
		letter-spacing: 0.04em;
		margin: 0 0 0.38rem !important;
		text-transform: uppercase;
	}

	.project-contrib-list {
		color: #d8ecff;
		display: grid;
		font-size: 0.83rem;
		gap: 0.24rem;
		margin: 0;
		padding-left: 1rem;
	}

	.detail-list,
	.award-list,
	.publication-list {
		display: grid;
		gap: 0.42rem;
		margin: 0;
		padding-left: 1.06rem;
	}

	.detail-list {
		color: #d8ecff;
		font-size: 0.93rem;
	}

	.resource-list {
		display: flex;
		flex-wrap: wrap;
		gap: 0.4rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.pill-link {
		background: transparent;
		border: 1px solid rgba(103, 189, 255, 0.35);
		border-radius: 0.35rem;
		color: #e8f8ff;
		display: inline-block;
		font-size: 0.81rem;
		font-weight: 700;
		padding: 0.3rem 0.64rem;
		text-decoration: none;
		transition: transform 0.2s ease, background-color 0.2s ease;
	}

	.pill-link:hover {
		background: rgba(88, 185, 255, 0.2);
		transform: none;
	}

	.video-wrap {
		border: 1px solid rgba(112, 182, 255, 0.28);
		border-radius: 0;
		overflow: hidden;
		transition: transform 0.28s ease, border-color 0.28s ease;
	}

	.project-card:hover .video-wrap {
		border-color: rgba(145, 210, 255, 0.56);
		transform: scale(1.01);
	}

	.video-wrap iframe {
		aspect-ratio: 16 / 9;
		border: 0;
		display: block;
		width: 100%;
	}

	.award-list {
		list-style: disc;
	}

	.award-list a,
	.contact-grid a,
	.section-shell a {
		color: #91e1ff;
		font-weight: 700;
		text-decoration: none;
	}

	.award-list a:hover,
	.contact-grid a:hover,
	.section-shell a:hover {
		text-decoration: underline;
	}

	.skills-chart-shell {
		align-items: start;
		display: grid;
		gap: 0.9rem;
		grid-template-columns: 1fr;
		justify-items: center;
	}

	.skills-visual {
		display: grid;
		gap: 0.45rem;
		justify-items: center;
		width: 100%;
	}

	.skills-sunburst {
		display: block;
		filter: drop-shadow(0 12px 20px rgba(2, 9, 24, 0.42));
		margin: 0 auto;
		max-width: 32rem;
		overflow: visible;
		width: 100%;
	}

	:global(.sunburst-parent) {
		cursor: pointer;
		stroke: rgba(2, 8, 24, 0.82);
		stroke-width: 1.4;
		transition: transform 0.2s ease, stroke 0.2s ease, filter 0.2s ease, opacity 0.2s ease;
	}

	:global(.sunburst-parent:hover) {
		filter: brightness(1.06);
	}

	:global(.sunburst-parent.is-active) {
		filter: brightness(1.1);
		stroke: rgba(224, 245, 255, 0.95);
		stroke-width: 1.8;
	}

	:global(.sunburst-parent:focus),
	:global(.sunburst-parent:focus-visible) {
		outline: none !important;
	}

	:global(.sunburst-parent-label) {
		fill: #0a0f18;
		font-size: 9.6px;
		font-weight: 700;
		letter-spacing: 0.01em;
		pointer-events: none;
		paint-order: stroke;
		stroke: rgba(255, 255, 255, 0.34);
		stroke-width: 0.45;
		user-select: none;
	}

	:global(.sunburst-child) {
		stroke: rgba(2, 10, 24, 0.62);
		stroke-width: 1;
		transition: filter 0.2s ease;
	}

	:global(.sunburst-child:hover) {
		filter: brightness(1.08);
	}

	:global(.sunburst-child-label) {
		fill: #0a0f18;
		font-size: 7.2px;
		font-weight: 700;
		pointer-events: none;
		stroke: rgba(255, 255, 255, 0.32);
		stroke-width: 0.35;
		paint-order: stroke;
		user-select: none;
	}

	:global(.sunburst-hover-chip) {
		pointer-events: none;
	}

	:global(.sunburst-hover-chip-bg) {
		fill: rgba(2, 10, 24, 0.9);
		stroke: rgba(166, 220, 255, 0.62);
		stroke-width: 0.8;
	}

	:global(.sunburst-hover-label) {
		fill: #f4fbff;
		font-size: 8.6px;
		font-weight: 800;
		letter-spacing: 0.01em;
		pointer-events: none;
		user-select: none;
	}

	:global(.sunburst-core) {
		fill: rgba(2, 10, 26, 0.92);
		stroke: rgba(162, 220, 255, 0.42);
		stroke-width: 1;
	}

	.skills-panel {
		display: grid;
		gap: 0.7rem;
		width: min(56rem, 100%);
	}

	.skills-legend {
		display: grid;
		gap: 0.36rem;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.legend-button {
		align-items: center;
		background: rgba(9, 27, 62, 0.55);
		border: 1px solid rgba(122, 188, 255, 0.25);
		border-radius: 0.34rem;
		color: #e9f8ff;
		cursor: pointer;
		display: grid;
		gap: 0.42rem;
		grid-template-columns: auto 1fr auto;
		padding: 0.42rem 0.58rem;
		text-align: left;
		transition: border-color 0.2s ease, background-color 0.2s ease;
		width: 100%;
	}

	.legend-button:hover {
		background: rgba(15, 39, 84, 0.74);
		border-color: rgba(154, 221, 255, 0.58);
	}

	.legend-button.is-active {
		background: rgba(20, 48, 100, 0.86);
		border-color: rgba(175, 231, 255, 0.84);
	}

	.legend-button:focus-visible {
		outline: 1px solid rgba(221, 245, 255, 0.92);
		outline-offset: 1px;
	}

	.legend-swatch {
		background: var(--legend-color);
		border-radius: 999px;
		box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.28);
		height: 0.72rem;
		width: 0.72rem;
	}

	.legend-title {
		font-size: 0.82rem;
		font-weight: 600;
		line-height: 1.2;
	}

	.legend-meta {
		color: #a4ceee;
		font-size: 0.78rem;
		font-weight: 700;
	}

	.skills-active-copy {
		background: rgba(6, 20, 48, 0.64);
		border: 1px solid rgba(122, 188, 255, 0.28);
		border-radius: 0.42rem;
		display: grid;
		gap: 0.45rem;
		padding: 0.62rem 0.7rem;
	}

	.skills-active-title {
		font-size: 0.96rem;
		font-weight: 700;
		margin: 0 !important;
	}

	.skills-active-description {
		color: var(--text-soft);
		font-size: 0.87rem;
		line-height: 1.35;
		margin: 0 !important;
	}

	.skills-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.skills-tags li {
		background: rgba(25, 62, 121, 0.62);
		border: 1px solid rgba(122, 188, 255, 0.35);
		border-radius: 999px;
		color: #e8f8ff;
		font-size: 0.77rem;
		font-weight: 600;
		line-height: 1;
		padding: 0.26rem 0.54rem;
	}

	.logo-frame {
		align-items: center;
		background: linear-gradient(180deg, rgba(244, 250, 255, 0.94), rgba(217, 232, 248, 0.94));
		border: 1px solid rgba(171, 217, 255, 0.65);
		border-radius: 0.32rem;
		box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.32);
		display: flex;
		height: 88px;
		justify-content: center;
		overflow: hidden;
		padding: 0.56rem;
	}

	.logo-frame img {
		height: auto;
		max-height: 100%;
		max-width: 100%;
		object-fit: contain;
	}

	.education-card .detail-list {
		font-size: 0.86rem;
		gap: 0.28rem;
	}

	.education-card p {
		font-size: 0.86rem;
	}

	.publication-list {
		list-style: none;
		gap: 0.24rem;
		padding-left: 0;
	}

	.publication-item {
		display: block;
		margin: 0;
	}

	.publication-line {
		line-height: 1.4;
		margin: 0;
	}

	.publication-index {
		color: #9ecdf3;
		font-weight: 800;
	}

	.publication-authors {
		color: #deecfb;
	}

	.publication-author-highlight {
		color: #ffffff;
		font-weight: 800;
	}

	.publication-paper-title {
		color: #f6fbff;
		font-weight: 700;
	}

	.publication-venue {
		color: var(--text-soft);
	}

	.publication-links {
		color: #a9c9e8;
		opacity: 0.94;
	}

	.publication-links a {
		color: #9ecdf3;
		font-weight: 500;
		text-decoration: none;
		border-bottom: 1px dotted rgba(158, 205, 243, 0.7);
	}

	.publication-links a:hover {
		color: #c5e7ff;
		border-bottom-color: rgba(197, 231, 255, 0.9);
	}

	.contact-layout {
		display: grid;
		gap: 1rem;
		grid-template-columns: minmax(16rem, 1fr) minmax(16rem, 1fr);
	}

	.map-wrap {
		background: transparent;
		border: 1px solid var(--line);
		border-radius: 0;
		overflow: hidden;
	}

	.map-wrap iframe {
		border: 0;
		display: block;
		height: 100%;
		min-height: 296px;
		width: 100%;
	}

	.contact-grid {
		display: grid;
		gap: 0.8rem;
		grid-template-columns: repeat(auto-fit, minmax(12rem, 1fr));
	}

	.contact-grid h3 {
		font-size: 1rem;
		margin-bottom: 0.4rem;
	}

	.contact-grid p {
		margin: 0.2rem 0;
	}

	.social-links {
		align-items: center;
		display: flex;
		gap: 0.55rem;
	}

	.social-link {
		align-items: center;
		border: 1px solid rgba(145, 225, 255, 0.48);
		border-radius: 999px;
		display: inline-flex;
		height: 2.1rem;
		justify-content: center;
		text-decoration: none;
		transition: background-color 0.2s ease, border-color 0.2s ease, color 0.2s ease, transform 0.2s ease;
		width: 2.1rem;
	}

	.social-link svg {
		height: 1.2rem;
		width: 1.2rem;
	}

	.social-link.social-link--whatsapp {
		background: #25d366;
		border-color: rgba(95, 235, 154, 0.9);
		color: #ffffff;
	}

	.social-link:hover {
		background: rgba(84, 161, 222, 0.2);
		border-color: rgba(183, 233, 255, 0.9);
		color: #d6f3ff;
		text-decoration: none;
		transform: translateY(-1px);
	}

	.social-link.social-link--whatsapp:hover {
		background: #1ebf5d;
		border-color: #94f2bf;
		color: #ffffff;
	}

	.social-link:focus-visible {
		outline: 2px solid rgba(193, 236, 255, 0.95);
		outline-offset: 2px;
	}

	.site-footer {
		color: #bee4ff;
		padding: 1.2rem 0 0.2rem;
		text-align: left;
	}

	.site-footer p {
		margin: 0.2rem 0;
	}

	.footer-note {
		color: #8fc4f0;
		font-size: 0.84rem;
	}

	:global(html[data-theme='light']) .bg-image-layer {
		background-image: none;
		opacity: 0;
	}

	:global(html[data-theme='light']) .aurora::before {
		opacity: 0.04;
	}

	:global(html[data-theme='light']) .aurora::after {
		opacity: 0.03;
	}

	:global(html[data-theme='light']) .orb-one {
		background: radial-gradient(circle, rgba(68, 126, 196, 0.24) 0%, rgba(68, 126, 196, 0) 70%);
	}

	:global(html[data-theme='light']) .orb-two {
		background: radial-gradient(circle, rgba(58, 151, 192, 0.21) 0%, rgba(58, 151, 192, 0) 70%);
	}

	:global(html[data-theme='light']) .orb-three {
		background: radial-gradient(circle, rgba(106, 130, 199, 0.2) 0%, rgba(106, 130, 199, 0) 70%);
	}

	:global(html[data-theme='light']) .site-header {
		background: rgba(247, 251, 255, 0.84);
		border-bottom: 1px solid rgba(55, 106, 169, 0.16);
	}

	:global(html[data-theme='light']) .brand-name {
		background: none;
		color: #1b4a82;
		-webkit-text-fill-color: currentColor;
	}

	:global(html[data-theme='light']) .site-header a:hover {
		background: rgba(39, 101, 181, 0.12);
		color: #133967;
	}

	:global(html[data-theme='light']) .cv-link {
		border-color: rgba(42, 115, 198, 0.46);
		color: #123761 !important;
	}

	:global(html[data-theme='light']) .language-toggle {
		color: #123761;
	}

	:global(html[data-theme='light']) .language-toggle::after {
		background: rgba(255, 255, 255, 0.96);
		border-color: rgba(67, 122, 190, 0.42);
		box-shadow: 0 6px 16px rgba(28, 66, 114, 0.14);
		color: #123761;
	}

	:global(html[data-theme='light']) .theme-toggle:hover {
		background: transparent;
		color: #123761;
	}

	:global(html[data-theme='light']) .theme-toggle::after {
		background: rgba(255, 255, 255, 0.96);
		border-color: rgba(67, 122, 190, 0.42);
		box-shadow: 0 6px 16px rgba(28, 66, 114, 0.14);
		color: #123761;
	}

	:global(html[data-theme='light']) h1,
	:global(html[data-theme='light']) h2,
	:global(html[data-theme='light']) h3 {
		color: #10233e;
	}

	:global(html[data-theme='light']) h2:hover {
		background: linear-gradient(120deg, #2b70bf 0%, #68a4df 46%, #1f5da7 100%);
		background-clip: text;
		filter: drop-shadow(0 0 8px rgba(75, 140, 214, 0.36));
		text-shadow: 0 0 14px rgba(96, 157, 227, 0.32), 0 0 22px rgba(130, 184, 241, 0.24);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	:global(html[data-theme='light']) .hero-photo {
		border-color: rgba(84, 130, 185, 0.34);
		box-shadow: 0 10px 22px rgba(17, 46, 86, 0.14);
	}

	:global(html[data-theme='light']) .filter-button {
		border-color: rgba(65, 113, 174, 0.24);
		color: #23466f;
	}

	:global(html[data-theme='light']) .filter-button:hover {
		background: rgba(47, 115, 198, 0.12);
		border-color: rgba(47, 115, 198, 0.44);
	}

	:global(html[data-theme='light']) .filter-button.is-active {
		background: rgba(47, 115, 198, 0.16);
		border-color: rgba(47, 115, 198, 0.58);
		color: #123761;
	}

	:global(html[data-theme='light']) .media-group-card,
	:global(html[data-theme='light']) .project-card,
	:global(html[data-theme='light']) .leadership-card,
	:global(html[data-theme='light']) .education-card,
	:global(html[data-theme='light']) .legend-button,
	:global(html[data-theme='light']) .skills-active-copy {
		background: rgba(255, 255, 255, 0.88);
		border-color: rgba(73, 121, 181, 0.24);
	}

	:global(html[data-theme='light']) .legend-button {
		color: #1a3f6b;
	}

	:global(html[data-theme='light']) .media-group-card:hover,
	:global(html[data-theme='light']) .project-card:hover,
	:global(html[data-theme='light']) .leadership-card:hover,
	:global(html[data-theme='light']) .education-card:hover,
	:global(html[data-theme='light']) .legend-button:hover,
	:global(html[data-theme='light']) .legend-button.is-active {
		background: rgba(248, 252, 255, 0.98);
		border-color: rgba(56, 108, 173, 0.45);
	}

	:global(html[data-theme='light']) .project-card .year {
		background: rgba(53, 126, 212, 0.14);
		border-color: rgba(53, 126, 212, 0.28);
		color: #194171;
	}

	:global(html[data-theme='light']) .video-chip {
		background: rgba(88, 123, 215, 0.14);
		border-color: rgba(88, 123, 215, 0.32);
		color: #1b3f72;
	}

	:global(html[data-theme='light']) .media-kind-chip {
		background: rgba(224, 236, 250, 0.9);
		border-color: rgba(78, 128, 188, 0.3);
		color: #1b426f;
	}

	:global(html[data-theme='light']) .media-link-item {
		border-top-color: rgba(73, 121, 181, 0.2);
	}

	:global(html[data-theme='light']) .media-preview {
		border-color: rgba(84, 130, 185, 0.35);
	}

	:global(html[data-theme='light']) .media-link-title,
	:global(html[data-theme='light']) .gallery-label,
	:global(html[data-theme='light']) .award-list a,
	:global(html[data-theme='light']) .contact-grid a,
	:global(html[data-theme='light']) .publication-index,
	:global(html[data-theme='light']) .section-shell a {
		color: #1d5fa7;
	}

	:global(html[data-theme='light']) .media-link-title:hover {
		color: #0f3f78;
	}

	:global(html[data-theme='light']) .media-source,
	:global(html[data-theme='light']) .legend-meta,
	:global(html[data-theme='light']) .publication-links,
	:global(html[data-theme='light']) .footer-note {
		color: #5b7292;
	}

	:global(html[data-theme='light']) .legend-title,
	:global(html[data-theme='light']) .skills-active-title {
		color: #15385f;
	}

	:global(html[data-theme='light']) .publication-authors,
	:global(html[data-theme='light']) .detail-list,
	:global(html[data-theme='light']) .project-contrib-list {
		color: #1b3352;
	}

	:global(html[data-theme='light']) .publication-author-highlight,
	:global(html[data-theme='light']) .publication-paper-title {
		color: #10233e;
	}

	:global(html[data-theme='light']) .carousel-control {
		border-color: rgba(66, 117, 180, 0.4);
		color: #1b426f;
	}

	:global(html[data-theme='light']) .carousel-control:hover {
		background: rgba(47, 115, 198, 0.12);
		border-color: rgba(47, 115, 198, 0.56);
	}

	:global(html[data-theme='light']) .carousel-dot {
		background: rgba(83, 132, 193, 0.36);
	}

	:global(html[data-theme='light']) .carousel-dot.is-active {
		background: rgba(42, 115, 198, 0.86);
	}

	:global(html[data-theme='light']) .gallery-video-wrap {
		background: rgba(224, 236, 250, 0.84);
		border-color: rgba(83, 132, 193, 0.38);
	}

	:global(html[data-theme='light']) .gallery-video-fallback {
		color: #1d446f;
	}

	:global(html[data-theme='light']) .gallery-video-play {
		background: rgba(255, 255, 255, 0.78);
		border-color: rgba(42, 96, 167, 0.8);
	}

	:global(html[data-theme='light']) .gallery-video-play::after {
		border-left-color: rgba(42, 96, 167, 0.9);
	}

	:global(html[data-theme='light']) .gallery-video-wrap::after {
		background: rgba(255, 255, 255, 0.95);
		border-color: rgba(57, 113, 181, 0.52);
		color: #164273;
	}

	:global(html[data-theme='light']) .project-contrib-popover {
		background: #f7fbff;
		border-color: rgba(65, 116, 180, 0.5);
		box-shadow: 0 10px 20px rgba(21, 53, 96, 0.14);
	}

	:global(html[data-theme='light']) .project-contrib-title {
		color: #1d456f;
	}

	:global(html[data-theme='light']) .pill-link {
		border-color: rgba(73, 123, 184, 0.3);
		color: #174879;
	}

	:global(html[data-theme='light']) .pill-link:hover {
		background: rgba(47, 115, 198, 0.1);
	}

	:global(html[data-theme='light'] .sunburst-parent) {
		stroke: rgba(228, 242, 255, 0.95);
	}

	:global(html[data-theme='light'] .sunburst-parent.is-active) {
		stroke: rgba(17, 56, 102, 0.45);
	}

	:global(html[data-theme='light'] .sunburst-parent-label),
	:global(html[data-theme='light'] .sunburst-child-label) {
		fill: #132b47;
		stroke: rgba(255, 255, 255, 0.8);
	}

	:global(html[data-theme='light'] .sunburst-hover-chip-bg) {
		fill: rgba(247, 252, 255, 0.95);
		stroke: rgba(70, 118, 176, 0.56);
	}

	:global(html[data-theme='light'] .sunburst-hover-label) {
		fill: #123458;
	}

	:global(html[data-theme='light'] .sunburst-core) {
		fill: rgba(255, 255, 255, 0.96);
		stroke: rgba(71, 120, 180, 0.36);
	}

	:global(html[data-theme='light']) .skills-tags li {
		background: rgba(226, 238, 251, 0.9);
		border-color: rgba(78, 128, 188, 0.3);
		color: #1f4a79;
	}

	:global(html[data-theme='light']) .logo-frame {
		background: #ffffff;
		border-color: rgba(47, 73, 108, 0.18);
		box-shadow: none;
	}

	:global(html[data-theme='light']) .image-lightbox {
		background: rgba(239, 246, 255, 0.92);
	}

	:global(html[data-theme='light']) .lightbox-close,
	:global(html[data-theme='light']) .lightbox-control {
		background: rgba(255, 255, 255, 0.9);
		border-color: rgba(67, 118, 181, 0.46);
		color: #1a416f;
	}

	:global(html[data-theme='light']) .lightbox-control:hover {
		background: rgba(47, 115, 198, 0.2);
		border-color: rgba(47, 115, 198, 0.68);
	}

	:global(html[data-theme='light']) .lightbox-figure figcaption {
		color: #2a4d78;
	}

	:global(html[data-theme='light']) .social-link {
		border-color: rgba(76, 126, 187, 0.4);
		color: #1b497c;
	}

	:global(html[data-theme='light']) .social-link:hover {
		background: rgba(47, 115, 198, 0.14);
		border-color: rgba(47, 115, 198, 0.52);
		color: #164273;
	}

	:global(html[data-theme='light']) .site-footer {
		color: #23486f;
	}

	@keyframes rise {
		from {
			opacity: 0;
			transform: translateY(12px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes fadeSlide {
		from {
			opacity: 0.38;
			transform: translateX(8px);
		}
		to {
			opacity: 1;
			transform: translateX(0);
		}
	}

	@keyframes starDrift {
		from {
			transform: translate3d(0, 0, 0);
		}
		to {
			transform: translate3d(-120px, -160px, 0);
		}
	}

	@keyframes drift {
		from {
			transform: translate3d(0, 0, 0) scale(1);
		}
		to {
			transform: translate3d(-22px, -30px, 0) scale(1.06);
		}
	}

	@media (max-width: 70rem) {
		.site-header {
			border-radius: 0;
			grid-template-columns: 1fr;
			position: sticky;
			top: 0;
		}

		.project-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.media-groups-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.media-link-item.has-preview {
			grid-template-columns: minmax(6.6rem, 7.8rem) 1fr;
		}

		.leadership-grid {
			grid-template-columns: 1fr;
		}

		.education-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.skills-chart-shell {
			grid-template-columns: 1fr;
		}

		.skills-visual {
			margin: 0 auto;
			max-width: 29rem;
			width: 100%;
		}

		.brand-name {
			font-size: clamp(1rem, 3.2vw, 1.2rem);
		}

		.cv-link {
			width: fit-content;
		}

		.header-actions {
			justify-self: start;
		}

		.contact-layout {
			grid-template-columns: 1fr;
		}

		.hero-main {
			grid-template-columns: 1fr;
		}

		.hero-photo-wrap {
			align-self: start;
			justify-self: start;
		}

		.hero-photo {
			width: min(16rem, 62vw);
		}
	}

	@media (max-width: 38rem) {
		.page {
			padding: 0 0.95rem 3rem;
		}

		.project-grid,
		.education-grid,
		.leadership-grid,
		.media-groups-grid {
			grid-template-columns: 1fr;
		}

		.media-link-item.has-preview {
			grid-template-columns: 1fr;
		}

		.skills-legend {
			grid-template-columns: 1fr;
		}

		.project-card {
			padding: 0.62rem;
		}

		.orb-one,
		.orb-two,
		.orb-three {
			filter: blur(16px);
			opacity: 0.65;
		}
	}
</style>
